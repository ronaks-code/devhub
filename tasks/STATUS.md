STATE: ACTIVE — RESUMED BY RONAK 2026-07-15 (full software implementation authorized; hard gates held)
# STATUS — DevHub
<!-- SOURCE OF TRUTH. Any fresh Claude/Codex chat reads this FIRST, before touching code. -->
<!-- Update rule: edit this file in the SAME commit as the work it describes. Never let it drift. -->

Last updated: 2026-07-16 by M7-FORK-FOUNDATION (DIRECTED TASK on
`wip/devhub-background-runner` — M7 Task 1: RED-first cross-provider fork handoff model
in the engine, behind the existing default-off `crossProviderFork` flag; flag value
itself UNCHANGED (`false`), `nativeCodex`/`persistentClaude` also unchanged). New module
`packages/engine/src/providers/cross-provider-fork.ts` (+export from `providers/index.ts`):
`buildCrossProviderHandoffPreview`/`buildTransferredContext` build an ALLOWLISTED (not
blocklisted) transferred-context payload from an already-`registry.readTask`-read source
task — only `message` events, only `user`/`assistant` roles, only from a REVIEWED
(completed-status) turn survive, every surviving string re-run through `redactSecrets`,
and a hidden-reasoning marker regex drops any message that slips one in; everything else
(approval/permission `request` events carrying approval credentials, diagnostics, usage/
status/plan/activity chatter, `system`-role messages, in-flight/unreviewed turn output) is
never allowlisted in, so it never reaches the payload — proved by an adversarial fixture
task in the new test file that embeds an OpenAI-style key, a `Bearer` token, an
`auth=`-assignment, a hidden/chain-of-thought-tagged message, an approval-credential
carrying request event, and an in-flight second turn, then asserts none of that text (or
the request's identity) survives while the legitimate reviewed message does.
`sourceTaskContentHash` (sha256 over `canonicalProviderIndexJson` of the full task) is
captured at preview time and `commitCrossProviderHandoff` RE-READS the source via
`registry.readTask` and rejects (`SourceTaskMutatedError`) if the hash drifted — the
source is only ever read in this module, never mutated by any of its own code, so a real
mutation between preview and commit is the only way the check can fire; the module also
re-checks the `crossProviderFork` flag at commit time independent of preview time. The
new native TARGET task is created via the EXISTING `ProviderRegistry.startTask` (not a
bespoke path), so it goes through the registry's normal ownership/id checks; the commit
guards that the returned task's provider is the requested target provider (rejects same-
provider / same-native-id results as `HandoffTargetNotNativeError`). Cross-task references
use `taskLocator` (provider + home FINGERPRINT + native task id, from the existing
`provider-index/identity.ts`) everywhere in the preview and the link, never the raw
provider home path — asserted directly in tests via `JSON.stringify(...).not.toContain(
rawHomePath)` on both the preview and the link. Linkage is ADDITIVE and bidirectional:
`commitCrossProviderHandoff` returns a `CrossProviderHandoffLink` with both a `forSource`
and a `forTarget` view (`relation: "handoff-source" | "handoff-target"`, each carrying the
other side's locator), neither view mutates either task's own fields. New test file
`packages/engine/test/providers/cross-provider-fork.test.ts` (7 tests): default-off
rejection at every entry point, redaction/allowlist adversarial proof, source-immutability
rejection, native-target creation + bidirectional linkage, cross-provider-only guard (same-
provider target rejected at preview), flag-flip-between-preview-and-commit rejection, and
content-hash stability/change-detection. Full engine suite re-run at this tip in the SAME
commit: `vitest run` 2213/2213 (80/80 files, +1 new file/+7 new tests, no existing test
changed) + `tsc --noEmit` clean + `tsc --project test/provider-index/tsconfig.public-
surface.json` clean (untouched — the new module isn't part of that narrow provider-index
surface list). `git diff --check` clean; `git status --short` shows only the new module,
its test, and the one-line `providers/index.ts` export addition. Server/web untouched (no
UI/route wiring — that's a later M7 task); `nativeCodex`/`persistentClaude` flags and every
M6 slice flag unchanged. Landed on `wip/devhub-background-runner` only, per task
instructions — NOT `origin/main`.
PRIOR: M6-CODEXSTYLESHELL-CUTOVER (DIRECTED TASK on
`wip/devhub-background-runner` — Ronak authorized the umbrella default-on cutover this file's
prior entries flagged as the only remaining M6 [RONAK-GATE]. Flipped
`DEFAULT_DEVHUB_FEATURE_FLAGS.codexStyleShell` + all 8 M6 slice flags (`shellChrome`/
`taskRail`/`taskHeaderSetup`/`threadWorkspace`/`composerSurface`/`inspectorDock`/
`searchCommands`/`settingsSecondary`) false -> true as REQUESTED defaults in
`packages/engine/src/providers/feature-flags.ts`, mirroring the exact M3/M4/M5 cutover
pattern. Also had to extend `registerSettingsRoutes`'s `availableDevHubFeatures` /
`appliedDevHubFeatures` wiring in `packages/server/src/app.ts` to report all 9 M6 flags
unconditionally available+applied (pure client-composition flags, no server runtime
dependency) — without that, the generic requested/available/applied AND-clamp in
`packages/server/src/routes/settings.ts` would have kept every M6 flag resolved `false`
regardless of the new default, silently defeating the cutover. No M6 slice's own
`resolve*Mode` function changed; each still independently gates off its own flag, so the
existing per-slice byte-compatible rollback (an explicit stored `false` on ANY ONE flag)
is untouched and verified in isolation for every slice. Added/updated tests: engine
`feature-flags.test.ts` (new default expectations), new `packages/server/test/
m6-cutover.test.ts` (real `buildApp` wiring — default-on + 9 explicit-false-isolates-
one-flag cases spanning the 8 slices + the umbrella itself), new `apps/web/src/
App.m6-cutover.test.ts` (web composition — default-on mounts every slice's `devhub` mode
via its real `resolve*Mode` function; an explicit false per flag isolates exactly that
slice). Captured live DevHub Browser QA under `evidence/m6/cutover/` (isolated scratch
`CLAUDE_CONFIG_DIR`, real built `apps/web/dist` + `tsx` server, zero real datasets
touched): `GET /api/settings` on a fresh install resolves `codexStyleShell` + all 8
slices `true`; screenshots of Home/Browse/Settings under the flag-on `DevHubShell`
composition (dark `TaskRail`/`InspectorDock`/`Composer`/`SettingsRoute`), plus one
proving an explicit `shellChrome: false` instantly restores the exact legacy light
`TopBar`/rail while every other slice (incl. the still-dark `SettingsRoute`) stays
mounted — the isolated, non-destructive rollback the DoD required. Full suite re-run at
this exact tip in the SAME commit: engine `vitest run` 2206/2206 (79/79 files, unchanged
count — only one existing test file's expectations updated) + `tsc --noEmit` clean +
`tsc --project test/provider-index/tsconfig.public-surface.json` clean; server `vitest
run` 247/247 (12/12 files, +1 new file/+10 new tests) + `tsc --noEmit` clean; web `vitest
run` 497/497 (35/35 files, +1 new file/+10 new tests) + `tsc --noEmit` clean + `vite
build` succeeded, no errors. `git diff --check` clean. Landed on
`wip/devhub-background-runner` only, per task instructions — NOT `origin/main`.
PRIOR: M6-SLICE-EVIDENCE (DIRECTED TASK on `wip/devhub-background-runner`
— produced per-slice M6 gate evidence + independent SPEC/QUALITY/SECURITY reviews for Tasks
3-9. Found the fidelity-ledger already had Task 1-9 entries (the assigning prompt's premise
of "only Task 1+2 exist" was stale — all nine landed their own ledger section in their own
commit on 2026-07-15) and that the referenced evidence files live at the repo-root
`evidence/m6/<slice>/` path, not `.planning/devhub-codex-parity/evidence/m6/` (the latter is
genuinely empty; a path-naming drift, not missing work). Added a `gate-evidence-2026-07-16.md`
addendum per slice (taskheader/thread/composer/inspector/search-commands/settings-secondary/
task9-compose) with REAL current/min(1024)/narrow(768) `playwright-cli` screenshots (isolated
session, not the shared MCP browser — that one was locked by a concurrent sibling task) and
grep-verified visible-copy diffs against each slice's frozen copy dictionary, plus mirrored
addenda into `fidelity-ledger.md`. Independent SPEC review per slice: every slice's own
per-slice DoD row I checked source-level (provider-identity read-only text, fixed-provider
disclosure, frozen copy dictionaries, five-tab InspectorDock, separate Search/Commands
dialogs, field-group Settings) is genuinely implemented as claimed — SPEC GO for all seven.
Independent QUALITY/SECURITY review found two REAL, previously-unstated gaps, not
fabricated as green: (1) EVERY M6 3-9 test file (`TaskHeader/TaskSetup/ThreadWorkspace/
ActivityTimeline/Composer/InspectorDock/TaskSearchDialog/CommandDialog/SettingsRoute/
settings-ui/SecondaryNav/ChatHost/m6-compose`.test.ts) renders via `renderToStaticMarkup` or
pure-function assertions ONLY — zero `fireEvent`/`userEvent`/`dispatchEvent` live-DOM
interaction tests exist anywhere in M6, so every "keyboard/a11y Match" row in the ledger is a
static-markup string check or roving-focus-math check, never a live-interaction proof; this
does not block anything today (`taskHeaderSetup`/`threadWorkspace`/etc. all stay
requested-false) but MUST be closed with real `fireEvent`/`userEvent` tests before any slice
flag is proposed for its own default-on promotion. (2) `ThreadWorkspace` (Task 4) and
`SettingsRoute` (Task 8) have literally zero narrow/768/viewport test coverage — the 768
screenshot for each showed the DEMO FIXTURE'S OWN fixed-px harness wrapper overflowing
(traced live to `.dh-canvas-frame{width:900px}` / `.frame{width:820px}`, both absent from the
real component source), which is not proof the shipped narrow behavior is broken, but IS
proof it is genuinely unverified — significant for Task 8 specifically because its own DoD
names "no horizontal overflow" at narrow as an explicit acceptance line. QUALITY/SECURITY
verdict: GO for landing this evidence work (no source changed beyond the addenda/ledger; no
credential/secret exposure introduced), but NO-GO on treating any M6 3-9 slice's `V`/keyboard
gate as fully closed until (a) a real `fireEvent`/`userEvent` interaction test lands per
slice and (b) `ThreadWorkspace`/`SettingsRoute` get an actual narrow-viewport test/fixture
(not a fixed-px demo card). No production source touched; `nativeCodex`/`persistentClaude`/
every M6 slice flag/`codexStyleShell` stay requested-false throughout. Re-ran the full suite
at this exact wip tip in the SAME commit: engine `vitest run` 2206/2206 (79/79 files) +
`tsc --noEmit` clean; server `vitest run` 237/237 (11/11 files) + `tsc --noEmit` clean; web
`vitest run` 487/487 (34/34 files) + `tsc --noEmit` clean + `vite build` succeeded.
`git diff --check` clean. PRIOR: DEVHUB-STATUS-RECONCILE (DIRECTED TASK on
`wip/devhub-background-runner` — reconciled the M6 drift this file had: line 53 claimed "Tasks 2-9 not started" but the on-disk tree (`apps/web/src/components/features/**`) and git log `4b97893..ad2f3e5` show all nine M6 slices built+tested behind their own default-OFF flags — `shellChrome`(T1)/`taskRail`(T2)/`taskHeaderSetup`(T3)/`threadWorkspace`(T4)/`composerSurface`(T5)/`inspectorDock`(T6)/`searchCommands`(T7)/`settingsSecondary`(T8), plus `M6-T9-COMPOSE` wiring all eight into `App.tsx`; the umbrella `codexStyleShell` and every slice flag remain requested-false in `packages/engine/src/providers/feature-flags.ts` exactly as before — verified by reading that file directly, so no source behavior changed, only the stale prose. Updated the M6 core-task line and the M6 Blocked entry to state the real built state and that the only remaining [RONAK-GATE] is the umbrella default-on cutover decision, not software readiness. Re-ran the full suite at this exact wip tip in the SAME commit: engine `vitest run` 2206/2206 passed (79/79 files) + `tsc --noEmit` clean + `tsc --project test/provider-index/tsconfig.public-surface.json` clean; server `vitest run` 236/236 passed (11/11 files) + `tsc --noEmit` clean; web `vitest run` 487/487 passed (34/34 files) + `tsc --noEmit` clean + `vite build` succeeded (built in 3.98s, no errors). `git diff --check` clean. PRIOR: M4-HANDSHAKE-FIX (DIRECTED TASK on `wip/devhub-background-runner` — fixed the `ClaudeCliProcess` INIT_TIMEOUT root cause logged in the M4 entry below: `start()` no longer gates readiness on a pre-turn `system/init` stdout event (CLI 2.1.207 never emits one before the first turn); it now resolves on the SDK `control_request{subtype:'initialize'}`→`control_response` handshake (empirically ~1.4s), sent unconditionally right after spawn via a new `beginInitializeHandshake()`/`tryConsumeInitializeResponse()` pair in `cli-process.ts` (new `INIT_FAILED` code for an explicit CLI rejection, `INIT_TIMEOUT` kept for the deadline). Session-identity validation (`captureSessionId`) is unchanged in substance but no longer gates startup — since CLI 2.1.207 only ever emits `system/init` during turn 1, a mismatch now surfaces there (still `MALFORMED_FRAME`, still fail-closed) via the existing turn-active/`onProcessTerminal` path instead of blocking `start()`. `task-runtime.ts` needed NO changes — it already treats `system/init` as a turn-scoped event via the process-factory seam. Scope held exactly as directed: NO flag flips, NO `lifecycleEvidence` wiring — the six M4 proofs are still pending, next. `cli-process.ts` test suite rewritten for the new handshake (was 41, now 48 tests, all green: +`fails closed when the CLI rejects the initialize handshake`, +`ignores control_response frames that don't correlate to the initialize handshake`, 3 identity/readiness tests rewritten to match the new gate). Full engine suite 2206/2206 green, `tsc --noEmit` clean, provider-index public-surface check clean, `git diff --check` clean. See full entry below.). PRIOR: DEVHUB-SHADCN (RONAK-DIRECTED — installed shadcn/ui + Radix as the app's sanctioned component library on `wip/devhub-background-runner`: deps + `@/` alias in tsconfig/vite/vitest + `components.json` + `src/components/ui/*` primitives on `cn` + additive `@theme inline` shadcn tokens mapped to the existing dark palette; `TaskRail` row className now flows through `cn`; +9 `ui-primitives.test.ts`; web 487/487, typecheck + `vite build` PASS. The pixel-locked M6 slices were deliberately NOT reimplemented on styled Radix primitives — that would silently break the design-lock visuals while DOM-only tests stayed green; see the DEVHUB-SHADCN task entry. All hard-gate holds intact. See full entry below.). PRIOR: M6-T9-COMPOSE (Wired the data-wires Tasks 1-8 explicitly deferred to Task 9, WITHOUT the cutover — this is the non-cutover half of M6 Task 9 on `wip/devhub-background-runner`. `apps/web/src/App.tsx` now feeds each already-built, already-QA'd Task 1-8 component REAL live data under its own default-OFF slice flag, each with its exact legacy owner as the instant flag-off rollback: `taskRail`'s `TaskRail.sections` is now the active project's real, most-recent-first sessions (`buildTaskRailSections`, provider always the honest `anthropic` legacy encoding — a native task row is a separate, still-gated data-wire) instead of the empty `[]` Task 2 shipped; Browse (`taskHeaderSetup`/`threadWorkspace`/`inspectorDock`) independently swaps `ProjectDetailHeader`→`TaskHeader` (real title/provider), `TranscriptPane`→`ThreadWorkspace` (real `ThreadItem[]` via a NEW `mapMessagesToThreadItems` — plain text becomes `user`/`assistant` prose, every other block becomes a bounded `raw` diagnostic, NEVER a fabricated tool card — `canSend=false` honestly since Browse has no live send capability), and adds `InspectorDock` (real `api.gitStatus` branch + `buildFileChanges`-derived Diff/Files content; Terminal/Browser/Artifacts honestly render `Not available`/`No artifacts`, no backing runtime in Browse); `searchCommands` replaces `SearchPalette` with `TaskSearchDialog` backed by a REAL debounced `/api/search` fetch (same endpoint, mapped via NEW `searchHitToResult`/`legacyDestinationForTarget`) and mounts `CommandDialog` for the FIRST TIME EVER (confirmed by grep: the legacy `CommandPalette` was imported but never rendered — always-dead code, not something this task broke) wired to the 5 approved `DEFAULT_COMMANDS` rows with real handlers (`New task`/`Search tasks`-transitions-to-Search/`Toggle inspector`-via-a-NEW-`inspectorVisible`-flag/`Open Settings`/`Go to Ops`); `settingsSecondary` actually mounts Task 8's own `SettingsRoute`/`OpsRoute`/`InboxRoute`/`DashboardRoute` in the Settings/Ops/Inbox/Dashboard tabs (same props each already accepted — Task 8's own STATUS entry said this live mount was deferred to Task 9). The Chat tab's `taskHeaderSetup`+`threadWorkspace`+`composerSurface` trio bundle ONE inseparable region inside the user-owned `ChatPane.tsx` (header/transcript/composer aren't independently extractable without editing that file), so a NEW `apps/web/src/components/features/shell/ChatHost.tsx` (not user-owned) — a from-scratch adapter over the SAME real `openChat` transport `ChatPane` uses — mounts ONLY when a NEW exported `resolveChatHostMode` sees all three resolve `devhub` together (a conservative AND, not three independent ORs; an explicit stored false on ANY ONE of the three still restores `ChatPane` untouched, honoring each flag's own instant-rollback promise for the one region that structurally can't be split); `ChatHost` sends real `{t:"prompt",cwd,prompt,sessionId,model,permissionMode}` frames and receives real `NormalizedMessage`s, with the same honest raw-diagnostic fallback for anything it doesn't render richly, and the honest Stop-gating `Composer`'s own `resolveSendState` already specifies (Stop never appears for Claude — `persistentClaude` stays false). `Composer`/`ThreadWorkspace`/`InspectorDock`/`TaskSearchDialog`/`CommandDialog` each gained a small number of ADDITIVE, OPTIONAL interaction props (`onDraftChange`/`onKeyDown`/`onSend` on `Composer`; `composerSlot` override on `ThreadWorkspace`; `onSelectDestination` on `InspectorDock`; `onQueryChange`/`onScopeChange`/`onDateFacetChange`/`onRetry`/`onClose` on `TaskSearchDialog`; `onQueryChange`/`onClose` on `CommandDialog`) that were simply absent from their Task 1-8 presentation-only shells (every prop defaults to producing the EXACT prior inert/`readOnly` markup when omitted). NEW `apps/web/src/lib/m6-compose.ts` holds every pure App-state→component-model adapter (`buildTaskRailSections`, `searchHitToResult`/`legacyDestinationForTarget`, `buildEnvironmentSummary`/`buildDiffContent`/`buildFilesContent`, `mapMessagesToThreadItems`), unit-tested in isolation so the mapping logic is verifiable without mounting React. TESTS: `lib/m6-compose.test.ts` (18, new) + `App.m6-t9.test.ts` (3, new, covers `resolveChatHostMode`'s conservative-AND contract) + every Task 1-8 slice's own test file passing UNMODIFIED (proving the additive props are truly backward-compatible). FULL suites GREEN: web 478/478 (33 files, +21), server 236/236 (11 files, unchanged), engine 2204/2204 (79 files, unchanged); all 3 package typechecks (engine/server/web) + `vite build` PASS; `git diff --check` clean; zero NUL/control bytes verified byte-by-byte in every touched/new file; four user-owned paths (`.gitignore`, `AGENTS.md`, `ChatPane.tsx`, `SlashPalette.tsx`) confirmed BYTE-IDENTICAL (`git diff --stat HEAD -- <path>` reports 0 lines for all four — none were even touched, so no hash re-verification was needed). Evidence: `evidence/m6/task9-compose/qa-note.md` (no new screenshots claimed — every mounted component is the SAME, already-QA'd Task 1-8 presentation; this note proves the WIRING, not new pixels); `fidelity-ledger.md` gained the "M6 Task 9 (compose half)" sub-table (7 rows, all Match) + judgment. SCOPE (honest): this is explicitly the NON-CUTOVER half of Task 9 per the m6-implementation-plan.md split — `codexStyleShell`'s requested default stays FALSE, `apps/web/src/components/ui.tsx` is NOT removed (its `D` gate — zero imports — was not run; it still has real importers), the full induced-failure drill suite and fresh independent specification/design/quality/security review of an exact cutover tip were NOT run, `ChatHost`'s richer `ChatPane` parity (permission-card approval UI, image attach, mention/slash picker dropdown rendering, token meter) was NOT attempted (a real, larger follow-up, not fabricated here), and Ops/Inbox/Dashboard's TopBar/TaskRail PRIMARY-tab labels are unchanged (only their tab CONTENT now routes through `SecondaryNav` when the flag is on) — restructuring primary nav itself into secondary-only destinations is cutover-task scope. NOT promoted to `campaign/auto-improve`; the M6 milestone stays `[RONAK-GATE]`; every flag touched here stays at its Task 1-8 default (false); `nativeCodex`/`persistentClaude` stay false. PRIOR — M6-T8-SETTINGS-SECONDARY (Extracted the canonical `SettingsRoute` plus `OpsRoute`/`InboxRoute`/`DashboardRoute` behind a NEW default-OFF `settingsSecondary` slice flag on `wip/devhub-background-runner` — M6 Task 8, the eighth and final strangler slice, implementing `design-lock.md` §8 / `component-state-matrix.md` §13 / `surface-inventory.md` `RT-04`..`RT-07` / `SF-20`. NEW `apps/web/src/components/features/settings/SettingsRoute.tsx` reimplements Settings as accessible field groups — `Appearance` (Theme/Density), `Providers` (Default model, `Native Codex`/`Persistent Claude` switches, a provider-capability status `Table`, and a `Connection` `FieldSet` with explicit `Saved in this browser`/`Not saved` + `Not synced — stored only on this device, never sent to the server.` labeling), `Budget` (the one quick preserved soft-cap field), and `Permissions` (default permission mode) — built from a NEW hand-rolled `apps/web/src/components/features/settings/settings-ui.tsx` implementing the audited `Tabs`/`FieldGroup`/`Field`/`FieldSet`/`Select`/`Input`/`Switch`/`Button`/`Alert`/`Progress`/`Table`/`Dialog` primitive set named by `surface-inventory.md` `RT-07` (never a generic bordered form card; `Tabs` reuses the EXISTING shared `nextTabIndex` roving-focus helper from `InspectorDock` rather than re-deriving the same math a third time). Every preserved workflow tab (Budget/Memory/MCP servers/Hooks/Webhooks/Permissions/Agents/Skills/Plugins, plus the RebuildIndex/IntegrityPanel/ArchiveTransfer maintenance utilities) is mounted UNCHANGED, and the whole save/reconcile/dirty-field state machine is REUSED from `SettingsPane`'s own exported pure helpers (`completeDevHubFeatures`, `withNativeCodexPreference`, `withPersistentClaudePreference`, `settingsUpdatePayload`, `dirtySettingsUpdatePayload`, `mergeAuthoritativeSettings`, `deliverSettingsResponse`, `requestSettingsReconciliation`) instead of re-deriving the same save/reconcile contract a second time. A NEW, real, bounded destructive action — `Clear local connection data` — exercises the `Dialog` primitive honestly per `component-state-matrix.md` §13's destructive rule: `describeClearLocalDataConfirmation()` proves `callsProviderDelete:false`, `initialFocus:"cancel"`, `reversible:true`, names the exact affected store (`devhub:conn`, this browser only), and the confirming dialog is absent until the user opens it; no equivalent control existed in the legacy panes to route through `Dialog` otherwise. NEW `apps/web/src/components/features/shell/SecondaryNav.tsx` is the shared secondary-navigation landmark (`role="list"` of `Settings`/`Live ops`/`Inbox`/`Dashboard`, `aria-current="page"` on the active one, zero logos) that NEW `apps/web/src/components/features/ops/OpsRoute.tsx`, `features/inbox/InboxRoute.tsx`, and `features/analytics/DashboardRoute.tsx` each wrap around the PRESERVED `LiveOpsBoard`/`InboxPane`/`DashboardPane` (reused UNCHANGED, not rewritten — every current running/triage/analytics behavior stays exactly as shipped; only the navigation placement changes from a primary task-home tab to a secondary destination, per matrix §13's rest rule "Ops/Inbox/Dashboard stay secondary utilities, not task-home cards" and `surface-inventory.md` `RT-06`'s "Never move dashboard cards into task shell"); all three re-export the SAME `resolveSettingsSecondaryMode`/`isSettingsSecondaryApplied` gate `SettingsRoute` owns — one slice, one flag, not four independent ones. The `settingsSecondary` flag was added (default false) to the engine `DevHubFeatureFlags` + `DEFAULT_DEVHUB_FEATURE_FLAGS`, the server settings-route `FEATURE_KEYS`/`completeFeatures`/`ALL_FEATURES_APPLIED`/JSON-schema, and the web `completeDevHubFeatures`; every exact-shape flag literal was bumped (`toHaveLength(13)`→`14`) across engine feature-flags, server settings-features, and web App/SettingsPane native-codex tests. RED-FIRST: `SettingsRoute.test.ts` (19) + `settings-ui.test.ts` (9) + `SecondaryNav.test.ts` (5) + `OpsRoute.test.ts`/`InboxRoute.test.ts`/`DashboardRoute.test.ts` (2 each) were written against the primitives/gate/routing contract, including the flag gate proving flag-off keeps `SettingsPane`/`LiveOpsBoard`/`InboxPane`/`DashboardPane` byte-identical and never instantiates the new tree (asserted structurally: no new component is reachable without an explicit true `settingsSecondary`). FULL suites GREEN with the new field: web 457/457 (31 files, +39), server 236/236 (11 files), engine 2204/2204 (79 files); all three package typechecks + `vite build` PASS; `git diff --check` clean; zero NUL/control bytes in authored files; four user-owned paths (`.gitignore`, `AGENTS.md`, `ChatPane.tsx`, `SlashPalette.tsx`) BYTE-IDENTICAL (hashes re-verified: 03479b6/28a7929/cfecb54/737110f). DevHub-OWN Browser QA (Playwright against a self-contained `fixture.html` built from the SHIPPED `index.css` + REAL `SettingsRoute`/`OpsRoute`/`InboxRoute`/`DashboardRoute` SSR markup, served on :5311, 1200x2600, dark): live DOM measurements across 5 labeled sections (Settings populated, Settings loading, Ops, Inbox, Dashboard) — exactly 4 field groups `Appearance`/`Providers`/`Budget`/`Permissions` as labelled `<section>`s with `border:0px none` (no card chrome), 5 `SecondaryNav` landmarks each with the correct `aria-current="page"` destination, 1 `role="tablist"`/10 `role="tab"` (every preserved workflow reachable), a 3-row provider-capability `Table`, local-vs-synced text `Not saved` + `Not synced — stored only on this device, never sent to the server.`, a real `<fieldset><legend>Connection</legend>`, Dashboard's own `aria-label="Loading dashboard"` rendered under `SecondaryNav` (never task-canvas chrome), 0 `<svg>`/`<img>` in the nav destination list, and the `Clear local connection data` button present while its `Dialog` is absent until opened — 10 comparison points, all Match; only benign `@import`/favicon 404s. Evidence: `evidence/m6/settings-secondary/{qa-note.md,fixture.html,m6-settings-secondary-wide.png}`; `fidelity-ledger.md` gained the M6 Task-8 sub-table (11 rows, all Match) + judgment. SCOPE (honest): this claims the Settings field-group PRESENTATION, preserved-workflow reachability, Ops/Inbox/Dashboard secondary-navigation placement, local-vs-synced/destructive-confirmation copy, and flag safety only; live mounting into `App.tsx`'s primary-tab rail (replacing the current `home`/`browse`/`settings`/`dashboard`/`ops`/`inbox` sibling tabs with `SecondaryNav`-routed destinations, wiring real handlers/routing) is a DEFERRED data-wire staged for the Task 9 `codexStyleShell` cutover, mirroring exactly how Tasks 1–7 left their live mount as a later data-wire. TOOLING (honest): same as every prior M6 task — no shadcn/cmdk/Radix packages are installed anywhere in this repo; `settings-ui.tsx` is a hand-rolled, ARIA-correct implementation of the exact named primitive set, matching the established convention rather than an inconsistent one-off partial adoption. NOT promoted to `campaign/auto-improve`; the M6 milestone stays [RONAK-GATE]; `nativeCodex`/`persistentClaude`/`searchCommands`/`settingsSecondary` requested-defaults stay false. PRIOR — M6-T7-SEARCH-COMMANDS (Extracted the canonical `TaskSearchDialog` + the separate `CommandDialog` behind a NEW default-OFF `searchCommands` slice flag on `wip/devhub-background-runner` — M6 Task 7, the seventh strangler slice, implementing `design-lock.md` §8 / `component-state-matrix.md` §12 / `surface-inventory.md` `SF-18`/`SF-19` / `design-system.md` §7.9. Found the flag plumbing + both new component files already present as UNCOMMITTED WIP in the worktree (pre-existing, well-tested); this task verified, fixed, and completed it rather than rewriting from scratch. NEW `apps/web/src/components/features/search/TaskSearchDialog.tsx` is the dedicated `Search tasks and messages` dialog: focused query, Global/current-project scope (radio semantics, Project disabled+reasoned absent a project), five locked date facets + after/before/clear controls, a result count/status region, and provider-locked result rows (title/project/highlighted-`<mark>`-snippet) whose provider is derived SOLELY from the composite native task key (`providerFromTaskKey`/`navigationTargetForResult`, which throw rather than infer a provider from text) — a degraded raw OpenAI session is labeled `OpenAI` (never `Codex`) and discloses `Read-only fallback`. A read failure renders a DISTINCT `role="alert"` region (`resolveSearchStatus` proves error always wins over empty, never collapsing to `No results`) that retains query/facets; a request in flight renders content-shaped `Skeleton` placeholder rows (reusing the existing shared `Skeleton` primitive) instead of a bare spinner. NEW `apps/web/src/components/features/commands/CommandDialog.tsx` is the SEPARATE `Search commands and tasks` action palette: exactly the five approved rows (`New task`/`Search tasks`/`Toggle inspector`/`Open Settings`/`Go to Ops`) with a subsequence fuzzy filter (`filterCommands`), a `providerScoped`+`capable` gate so a command can never silently invoke another provider (`visibleCommands`), and its `Search tasks` row CLOSES Commands then OPENS Search (`describeSearchTasksTransition`/`isSearchTasksAction`) rather than rendering Search inline; `describeEscapeRestore` names the invoker so Escape restores focus. Both dialogs share one `#2d2d2d`/12px-radius Dialog composition (additive `.dh-dialog*`/`.dh-search-*`/`.dh-command-*` CSS in `index.css` reusing existing `--dh-*` tokens) but are separate `role="dialog"` surfaces that never merge. The `searchCommands` flag was added (default false) to the engine `DevHubFeatureFlags` + `DEFAULT_DEVHUB_FEATURE_FLAGS`, the server settings-route `FEATURE_KEYS`/`completeFeatures`/`ALL_FEATURES_APPLIED`/JSON-schema, and the web `completeDevHubFeatures`; every exact-shape flag literal was bumped (`toHaveLength(12)`→`13`) across engine feature-flags, server settings-features, and web App/SettingsPane native-codex tests. Fixed two real `tsc --noEmit` errors surfaced by the pre-existing WIP (an unguarded `filtered[active]` index + a non-null test assertion) — both are possibly-undefined-under-`noUncheckedIndexedAccess` fixes, no behavior change. RED-FIRST ADDED: two new TaskSearchDialog tests (error region carries `role="alert"`; loading renders `3` `aria-hidden` Skeleton rows and never the plain placeholder) were written and confirmed FAILING against the pre-fix component, then the `role="alert"` + Skeleton-loading branch was implemented to turn them GREEN. TESTS: `TaskSearchDialog.test.ts` (26) + `CommandDialog.test.ts` (16), including the shared `resolveSearchCommandsMode`/`isSearchCommandsApplied` slice-flag gate (devhub only for an explicit true; legacy for false/undefined/missing) proving flag-off never instantiates either dialog. FULL suites GREEN with the new field: web 418/418 (25 files, +26), server 236/236 (11 files), engine 2204/2204 (79 files); all three package typechecks + `vite build` PASS; `git diff --check` clean; zero NUL/control bytes in authored files (only legitimate `—`/`§`/`·`/`…`); four user-owned paths (`.gitignore`, `AGENTS.md`, `ChatPane.tsx`, `SlashPalette.tsx`) untouched. DevHub-OWN Browser QA (Playwright against a self-contained `fixture.html` built from the SHIPPED `index.css` + REAL `TaskSearchDialog`/`CommandDialog` SSR markup, served on :5309, 1200x2600, dark): live measurements across 8 labeled sections (Search populated/project-scope/loading/empty/error, Commands default/filtered/empty) — shared `rgb(45,45,45)`=`#2d2d2d`/12px radius on both, Search+Commands never on one node, result-row min-height 36px, `3` `aria-hidden` Skeleton rows while loading, error `role="alert"` in `rgb(217,92,92)`=`--dh-danger`, active-row fill `rgb(49,49,49)`=`--dh-selected`, FTS `<mark>` tinted via `color-mix(...--dh-brand...)`, degraded row text `OpenAI`+`Read-only fallback` (never `Codex`), 0 svg/0 img — 10 comparison points, all Match; only a benign favicon 404. Evidence: `evidence/m6/search-commands/{qa-note.md,fixture.html,m6-search-commands-wide.png}`; `fidelity-ledger.md` gained the Task-7 sub-table (11 rows, all Match) + judgment. SCOPE (honest): this claims the Search + Commands PRESENTATION, provider-locked result/navigation contract, Commands→Search handoff, keyboard/a11y, and flag safety only; live mounting into `App.tsx`'s overlay tree (replacing the legacy `SearchPalette` mount + the currently-imported-but-unmounted `CommandPalette`, wiring real debounced fetch + real handlers) is a DEFERRED data-wire staged for the Task 9 `codexStyleShell` cutover, mirroring how Tasks 3–6 left their live mount as a later data-wire (only the outermost `shellChrome`/`taskRail` cutover points are live-switched in `App.tsx` today). TOOLING (honest): `design-lock.md` §7 names the shadcn/Radix/cmdk toolchain by name (Command/Dialog/ToggleGroup/Skeleton/Empty/Alert), but NO M6 task (1–7) has installed shadcn/cmdk/Radix packages anywhere in this repo (`apps/web/package.json` carries zero `@radix-ui`/`cmdk` deps; no `components.json`/`ui/` folder exists) — the entire `apps/web` codebase (100+ components including the legacy `SearchPalette`/`CommandPalette` this slice supersedes) uses hand-rolled, ARIA-correct primitives on the shared `cn()` token system. This slice follows that same established, lower-risk convention: it reuses the EXISTING shared `Skeleton` component for the loading state and a `role="alert"` region standing in for `Alert`; installing net-new shadcn/cmdk/Radix packages for this one slice alone, while every other M6 surface (and the rest of the app) stays hand-rolled, would be an inconsistent partial adoption — a full shadcn migration is an unstarted, larger follow-up explicitly out of scope for a single slice. NOT promoted to `campaign/auto-improve`; the M6 milestone stays [RONAK-GATE]; `nativeCodex`/`persistentClaude`/`searchCommands` requested-defaults stay false. PRIOR — M4-LIFECYCLE-PROOFS (Drove the REAL persistent-Claude drive path against the staged EXACT `2.1.207` arm64 binary via `DEVHUB_CLAUDE_EXECUTABLE` — `createNativeClaudeRuntime` + a directly-constructed `ClaudePersistentSupervisor` with `isEnabled:()=>true` — to find exactly where the six raw-lifecycle proofs (`persistentMultiQuery`/`rawResume`/`rawPermissionResponse`/`rawInterruptReceipt`/`postInterruptResume`/`forkContinuation`) become reachable. RESULT — the six proofs were NOT demonstrated and NO evidence was fabricated; `persistentClaude` stays false and no `lifecycleEvidence` was wired into `app.ts`. HONEST FINDING: the version-pin gate is now fully CLEARED — `discoverNativeClaudeInstallation` accepts the staged binary (`/private/tmp/devhub-m3m4-proof/arm64bin/package/claude`, home `/Users/ronak/.claude`) and it prints exactly `2.1.207 (Claude Code)` so the exact `--version` probe passes. The SOLE remaining blocker is a missing PROGRAMMATIC Anthropic credential: `createNativeClaudeRuntime(...)` returns `null` and a direct `supervisor.acquire(...)` throws `ClaudeSupervisorError` code `UNAUTHORIZED_AUTH` ("Claude persistent runtime requires programmatic authentication") from its internal `authorized()` gate — BEFORE any CLI launch — so `startTask`/`send`/`resumeTask`/`forkTask`/`interrupt`/`respond` are unreachable and not one billable turn can start. This is BY DESIGN: `auth-policy.ts` accepts only `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN`/Bedrock/Vertex/Foundry and explicitly rejects a stored Claude-app/Pro/Max OAuth login. Exhaustive check (names only): those keys are unset in the process env, in a login+interactive `zsh -lic` shell, and in `launchctl getenv`; no `~/.claude/.credentials.json`, no `apiKeyHelper` in settings.json, no repo `.env*`, no `ANTHROPIC_*` in shell profiles; the only Claude secret on the box is the Keychain "Claude Safe Storage" OAuth login, which the policy rejects. (M3 Codex succeeded because Codex accepts real `~/.codex` interactive auth; Claude's runtime does NOT accept the analogous interactive Claude OAuth, so the same "real local auth" does not unblock M4.) Zero billable turns spent (the auth gate fires before any launch); no scratch Claude task created; the throwaway probe `packages/server/m4-lifecycle-probe.mts` was deleted so the tree is clean. TO FINISH (no code change; do NOT weaken the gate): provide ONE real programmatic credential (e.g. a real `ANTHROPIC_API_KEY`) to the DevHub process env, then drive+capture the six proofs and only then wire the earned `lifecycleEvidence` into `app.ts`. HARD-GATE STOP: this needs a credential not present on this machine and not held by the agent. Evidence: `.planning/devhub-codex-parity/evidence/m4/lifecycle-proof-attempt-2026-07-15.md`. Hard gates (production cutover, `origin/main` merge, missing programmatic Claude credential) still held. PRIOR — M3-M4-LIVE-PROOF (Ran the authorized billable live-proof turns. M3 = PASS with real captured evidence: through the SHIPPING production seam (DevHub HTTP + installed Codex app-server `0.144.3`, real `~/.codex` auth, isolated `DEVHUB_DATA`, `nativeCodex` enabled at runtime via stored settings) drove a read-only scratch task `019f6727-2048-7c63-b37f-47db314e2adf` — turn 1 (billable) create+turn persisted nonce `M3-NONCE-A84AEE31`; then a REAL DevHub backend restart (killed `:8915`, relaunched fresh `tsx` process + fresh app-server against the same `DEVHUB_DATA`, native history survived); production-wrapper `POST /resume` returned HTTP 200 (verifier `verifyConfiguredResult` passed); turn 2 (billable) follow-up that did NOT repeat the nonce got the model to reply with exactly `M3-NONCE-A84AEE31`, proving continued conversation across restart. Root-caused the prior parked `409 provider_policy_mismatch` on resume: a temporary/since-reverted `DEVHUB_POLICY_DIAG` log in `verifyConfiguredResult` showed the ONLY failing check was `exactControlCwd` — a macOS `/tmp`→`/private/tmp` symlink artifact of the scratch cwd (top-level `cwd` realpath'd to `/private/tmp/...` while the persisted `thread.cwd` kept `/tmp/...`); DevHub's resume path is CORRECT and safely fail-closed, and a canonical (non-symlinked) cwd makes create+resume echo consistent cwds and pass — proven above. 3 real billable Codex turns spent (all authorized); both scratch rollouts removed from `~/.codex` and verified absent; the diagnostic edit to `codex/native-adapter.ts` reverted byte-identical to HEAD. `nativeCodex` requested-DEFAULT stays false (flipping the shipping default is the separate M8/RONAK cutover). Evidence: `.planning/devhub-codex-parity/evidence/m3/live-runtime-resume-proof.md`. M4 = NOT passed this run, characterized + made runnable, NO fabricated evidence: `persistentClaude` is gated by (1) an EXACT CLI version pin `2.1.207 (Claude Code)` (machine has `2.1.210`) — solved by staging the exact `2.1.207` arm64 native binary via `npm pack @anthropic-ai/claude-code-darwin-arm64@2.1.207` (npm here is x64/Rosetta and omits the arm64 dep; `npm pack` bypasses the cpu gate) which passes the version probe exactly, pointed at via `DEVHUB_CLAUDE_EXECUTABLE`; and (2) the real gate — `canEnable = lifecycleVerified` requires a `lifecycleEvidence` object (`cliVersion:2.1.207` + `persistentMultiQuery`/`rawResume`/`rawPermissionResponse`/`rawInterruptReceipt`/`postInterruptResume`/`forkContinuation` ALL true) that `app.ts` does not pass, so the persistent Claude runtime is locked closed BY DESIGN until the six raw-lifecycle proofs are demonstrated (directly against `ClaudeNativeAdapter`/`ClaudePersistentSupervisor`, since the HTTP routes share the same gate) and the EARNED evidence is wired into `createNativeClaudeRuntime` in `app.ts`. Confirmed live: even with the correct binary + requested true, `GET /api/settings` still resolves `persistentClaude:false`. The timing-sensitive permission/interrupt/post-interrupt/fork captures were not completed, so the evidence object was deliberately NOT fabricated. `persistentClaude` stays false. Evidence: `.planning/devhub-codex-parity/evidence/m4/live-gate-status-2026-07-15.md`. No tracked source changed; four user-owned preservation paths untouched; concurrent M6-T7 working-tree edits left unstaged/untouched. NOT promoted; hard gates (production cutover, origin/main merge) still held. PRIOR — M6-T6-INSPECTOR (Extracted the canonical `InspectorDock` behind a NEW default-OFF `inspectorDock` slice flag on `wip/devhub-background-runner` — M6 Task 6, the sixth strangler slice, implementing `design-lock.md` §8 / `component-state-matrix.md` §11 / `surface-inventory.md` `SF-11`..`SF-17`. NEW `apps/web/src/components/features/inspectors/InspectorDock.tsx` renders ONE measured 300-wide, content-height, rounded `#2d2d2d` dock (12 top / 16 right lane gutter, 16 inner padding, ~16 radius, geometry mirrored from `SHELL_GEOMETRY`) that is explicitly NOT a full-height IDE split pane. It opens with a persistent, compact, NON-TAB `Environment` summary region (backed environment/repository/subagent/source rows only — changes/branch/repo-actions/PR-status/subagents-or-`No active subagents`/sources) that is byte-identical across selections and rendered ABOVE the tablist, followed by EXACTLY five selectable destinations `Diff`/`Files`/`Terminal`/`Browser`/`Artifacts` on a `role="tablist"` (`aria-orientation="horizontal"`) with roving Left/Right/Home/End focus (pure `nextTabIndex`), a single roving `tabindex=0` tab, and a `tabindex=0` `tabpanel` labelled by the selected tab (Tab enters the panel); EXACTLY ONE destination renders, and the footer reads `Availability follows the task runtime`. Availability follows the REAL runtime not the schema via pure `computeDestinationView` (precedence: unsupported → cached → empty → content): a gated destination reads `Not available for this task` (with an appended cause when useful via `unavailableMessage`); empty-but-supported Artifacts reads `No artifacts` (a DISTINCT state, no unavailable node); a disconnected/stale panel reads cached with `Showing cached data — reconnect to refresh.` keeping cached content readable. Terminal is provider-emitted output ONLY (a `<pre>`, zero `<input>`/`<textarea>`/`<button>`) and the markup never references `thread/shellCommand` — no unsandboxed-shell fallback; Browser populates only from a real browser runtime (empty → `Not available for this task`). Destructive discard/unstage/worktree-deletion is a repository-utility confirmation OUTSIDE any tab (`describeDestructiveConfirmation`: `rendersInTab:false`, focus on `Cancel`, names the target, states the provider task is unaffected) and the dock renders NO destructive control inside a tabpanel. `ScrollArea`/overflow appears ONLY inside the bounded diff body (`data-dh-diff-scroll`, `max-height:220px`); narrow/PWA uses the titled `Desktop required for terminal and diff` disclosure variant (no tablist/tabpanel). Renders zero `<svg>`/`<img>`. Additive `.dh-inspector-dock`/`.dh-inspector-env*`/`.dh-inspector-tablist`/`.dh-inspector-tab`/`.dh-inspector-panel`/`.dh-inspector-diff-scroll`/`.dh-inspector-terminal`/`.dh-inspector-artifacts`/`.dh-inspector-footer`/`.dh-inspector-disclosure*` CSS added to `index.css` reusing existing `--dh-*` tokens (the root reuses the shared `.dh-inspector` surface; no legacy-token mutation). The `inspectorDock` flag was added (default false) to the engine `DevHubFeatureFlags` + `DEFAULT_DEVHUB_FEATURE_FLAGS`, the server settings-route `FEATURE_KEYS`/`completeFeatures`/`ALL_FEATURES_APPLIED`/JSON-schema, and the web `completeDevHubFeatures`; every exact-shape flag literal was bumped (`toHaveLength(11)`→`12`) across engine feature-flags, server settings-features, and web App/SettingsPane native-codex tests. TESTS: NEW `InspectorDock.test.ts` (31) — geometry mirrored from `SHELL_GEOMETRY` + content-height + byte-identical dock/env across selections, Environment-is-a-summary-not-a-tab, exactly five tabs + footer copy, tablist roving (`nextTabIndex`) + tabpanel entry + exactly-one-panel, `computeDestinationView` precedence, gated `Not available for this task` (+cause), distinct `No artifacts`, terminal output-only/no-shell/no-`shellCommand`, browser-only-from-runtime, disconnected cached read, destructive-never-in-a-tab + `describeDestructiveConfirmation`, ScrollArea-only-in-bounded-diff, narrow disclosure, and the `inspectorDock`/`isInspectorDockApplied` flag gate (flag-off never instantiates the dock). FULL suites GREEN with the new field: web 376/376 (+31), server 236/236, engine 2204/2204; all three package typechecks + `vite build` PASS; `git diff --check` clean; zero NUL/control bytes in authored files (only legitimate `—`/`§`/`·`/`…`); four user-owned paths (`.gitignore`, `AGENTS.md`, `ChatPane.tsx`, `SlashPalette.tsx`) BYTE-IDENTICAL (hashes re-verified: 03479b6/28a7929/cfecb54/737110f). DevHub-OWN Browser QA (Playwright MCP against self-contained fixtures built from the SHIPPED `index.css` + REAL `InspectorDock` SSR markup, served on :5219, dark): live measurements — dock width 300, content-height (321 in a 900 viewport, NOT full), fill `rgb(45,45,45)`=`#2d2d2d`, radius 16px, padding 16px, 5 tabs Diff/Files/Terminal/Browser/Artifacts, exactly 1 selected + 1 panel, Environment above tablist above panel, footer verbatim, diff scroll `overflow-y:auto`/`max-height:220px`, terminal `pnpm test`/`622 passed` with no input, browser gated `Not available for this task`, artifacts empty `No artifacts` (no unavailable node), disconnected `Showing cached data — reconnect to refresh.` with cached content readable, narrow disclosure `Desktop required for terminal and diff` (no tablist/tabpanel) — 16 comparison points, all Match; only benign `@import` 404s. Evidence: `evidence/m6/inspector/{qa-note.md,fixture-{diff,terminal,browser-unavailable,artifacts-empty,disconnected,disclosure}.html,m6-inspector-{diff,terminal,browser-unavailable,disconnected}-wide.png,m6-inspector-disclosure-narrow.png}`; `fidelity-ledger.md` gained the Task-6 sub-table (16 rows, all Match) + judgment. SCOPE (honest): this claims the inspector PRESENTATION, dock geometry, runtime-gated destination logic, keyboard/a11y, and flag safety only; live mounting into the shell + wiring real repository/provider events is a DEFERRED data-wire staged for the Task 9 `codexStyleShell` cutover, mirroring Tasks 1–5. NOT promoted to `campaign/auto-improve`; the M6 milestone stays [RONAK-GATE]; `nativeCodex`/`persistentClaude` stay false; `inspectorDock` requested-default stays false. PRIOR — M6-T5-COMPOSER (Extracted the canonical geometry-stable `Composer` behind a NEW default-OFF `composerSurface` slice flag on `wip/devhub-background-runner` — M6 Task 5, the fifth strangler slice, implementing `design-lock.md` §4/§6. NEW `apps/web/src/components/features/shell/Composer.tsx` reimplements the legacy `ChatPane` composer against the SAME registries/hooks (`useDraft`, `usePromptHistory`, `detectMention` from `MentionPicker`, `filterCommands` from `SlashPalette`, snippet append) WITHOUT editing the user-owned `ChatPane.tsx` or `SlashPalette.tsx`. It renders a measured 736x98 slot on the `#2d2d2d` surface (16 bottom gutter, ~21 radius) whose geometry-bearing container tag is BYTE-IDENTICAL across Send↔Stop and every draft/disabled transition (the stable-composer invariant): the send affordance carries `data-dh-send-state` but the root's `data-dh-composer-*` never depend on it or the item count. All non-presentational behavior is PURE functions so it is asserted without a DOM: `resolveSendState` (Stop shown ONLY when `turnRunning && nativeInterruptEnabled` — gated FALSE for Claude, so a running Claude turn honestly stays Send), `computeSendDisabledReason` (deterministic first-unmet reason for pending-creation/unsupported/disconnected-stale/missing-writer-lease/blocking-request/empty, the disconnected reason IS the reconnect note), `decideComposerKey` (Enter sends, Shift+Enter newline, boundary Up/Down history only while idle at the caret boundary, and an open mention/slash picker OWNS Arrow/Enter/Tab/Escape so focus never leaves the textarea — mirrors `ChatPane.onKeyDown` exactly), and `computePickerState`/`applySlashInsert`/`applyMentionInsert`/`appendSnippet` (registry-backed). The textarea has an EXPLICIT `<label for>` (not placeholder-only), the new-task placeholder is `Describe the outcome or change…`, and the draft stays editable even while disconnected (note `Reconnect to send. Your draft is saved.`) — never cleared until a send is accepted (owned by `useDraft`). The footer shows compact provider identity as QUIET TEXT (`OpenAI · Codex`/`Anthropic · Claude`, never a logo) plus provider-native folder/model/mode/permission context: the permission LABEL is derived from the provider (`PERMISSION_FIELD_LABEL`: Codex `Permissions`, Claude `Permission mode` — Claude NEVER `Workspace`) while permission/mode VALUES pass through verbatim (never cross-mapped by equality); credentials and an unsandboxed-shell fallback are never rendered (unit-asserted). Additive `.dh-composer*` CSS added to `index.css` reusing existing `--dh-*`/`--dh-composer-*` tokens (no legacy-token mutation; focus-within ring drawn without changing the box model). The `composerSurface` flag was added (default false) to the engine `DevHubFeatureFlags` + `DEFAULT_DEVHUB_FEATURE_FLAGS`, the server settings-route `FEATURE_KEYS`/`completeFeatures`/`ALL_FEATURES_APPLIED`/JSON-schema, and the web `completeDevHubFeatures`; every exact-shape flag literal was bumped (`toHaveLength(10)`→`11`) across engine feature-flags, server settings-features, and web App/SettingsPane native-codex tests. TESTS: NEW `Composer.test.ts` (26) — geometry mirrored from `SHELL_GEOMETRY` + stable-slot byte-identity across send/stop/draft/disabled, honest Stop gating, accessible label + outcome placeholder, provider-native footer never cross-mapped, each disabled reason + `aria-describedby` wiring, disconnect-editable draft, full keyboard contract + picker ownership, registry-backed picker/insert/snippet helpers, no-credential/unsandboxed-shell scan, and the `composerSurface`/`isComposerSurfaceApplied` flag gate. FULL suites GREEN with the new field: web 345/345 (+26), server 236/236, engine 2204/2204; all three package typechecks + `vite build` PASS; `git diff --check` clean; zero NUL/control bytes in authored files (only legitimate `—`/`§`/`·`/`…`); four user-owned paths (`.gitignore`, `AGENTS.md`, `ChatPane.tsx`, `SlashPalette.tsx`) BYTE-IDENTICAL (hashes re-verified). DevHub-OWN Browser QA (Playwright against self-contained fixtures built from the SHIPPED `index.css` + REAL `Composer` SSR markup, served on :5209, 1440x900 device-scale-2, dark): live measurements across resting/active/disconnected — composer 736x98, fill `rgb(45,45,45)`=`#2d2d2d`, radius 21px, margin-bottom 16px; Send↔Stop swaps label/state/`#d95c5c` only with geometry unchanged; Send disabled + `aria-describedby` reason `Write a message to send.`; textarea never disabled (draft preserved while disconnected) with note `Reconnect to send. Your draft is saved.`; placeholder `Describe the outcome or change…`; identity `Anthropic · Claude`/`OpenAI · Codex`; provider-native `Permission mode`/`plan` vs `Permissions`/`workspace-write`; 0 svg/0 img — 15 comparison points, all Match; only benign `@import` 404s, zero JS errors. Evidence: `evidence/m6/composer/{qa-note.md,fixture-{resting,active,new,disconnected}.html,m6-composer-{resting,active,disconnected}-wide.png}`; `fidelity-ledger.md` gained the Task-5 sub-table (12 rows, all Match) + judgment. SCOPE (honest): this claims the composer PRESENTATION, stable-slot geometry, provider-native footer, keyboard/disabled-reason logic, and flag safety only; live mounting into the task canvas (wiring the hooks/pickers to a running task) is a DEFERRED data-wire because the live composer host is the user-owned `ChatPane.tsx` (off-limits), mirroring exactly how Tasks 3–4 deferred their data-wires; the flag gate + resolver land here and the App composition mount is staged for the Task 9 `codexStyleShell` cutover. NOT promoted to `campaign/auto-improve`; the M6 milestone stays [RONAK-GATE]; `nativeCodex`/`persistentClaude` stay false; `composerSurface` requested-default stays false. PRIOR — M6-T4-THREAD-WORKSPACE (Completed + committed the ThreadWorkspace + ActivityTimeline strangler slice behind a NEW default-OFF `threadWorkspace` slice flag on `wip/devhub-background-runner` — M6 Task 4 (NOT promoted). `apps/web/src/components/features/shell/ThreadWorkspace.tsx` renders the transcript-IS-the-task canvas from `design-lock.md` §4/§6: completed and active work share ONE vertical narrative (`ThreadItem` union of assistant/user/activity/request/streaming/raw), with SURFACES reserved for the user bubble (`#242424`, right-aligned, capped at the measured 566), inline requests (`#262626`, `role="group"` NOT modal — no `role="dialog"`/`aria-modal`, per-turn actions only via `sanitizeRequestActions` which drops any `Always allow`), and the geometry-STABLE 736x98 composer (radius 21, 16 bottom gutter, `#2d2d2d`) whose `data-dh-composer-*` never depend on Send↔Stop or item count; everything else (assistant prose, activity rows, streaming deltas, `boundRawDiagnostic`-bounded/control-stripped raw unknown events capped at 2048) is UNFRAMED, the empty existing task is a genuinely BLANK canvas (zero children/SVG/hero/suggestions) while the composer + exactly one polite `role="status"` live region still render, and the region is NEVER wrapped in a shadcn `ScrollArea`; renders zero `<svg>`/`<img>`. `ActivityTimeline.tsx` is the compact inline plan/activity delegate. The `threadWorkspace` flag was added (default false) to the engine `DevHubFeatureFlags` + `DEFAULT_DEVHUB_FEATURE_FLAGS`, the server settings-route `FEATURE_KEYS`/`completeFeatures`/`ALL_FEATURES_APPLIED`/JSON-schema, and the web `completeDevHubFeatures`; every exact-shape flag literal was bumped (`toHaveLength(9)`→`10`) across engine feature-flags, server settings-features, and web App/SettingsPane native-codex tests. TESTS: `ThreadWorkspace.test.ts` (21) + `ActivityTimeline.test.ts` (9) GREEN, including the `resolveThreadWorkspaceMode`/`isThreadWorkspaceApplied` slice-flag gate (devhub only for an explicit true; legacy for false/undefined/missing). FULL suites GREEN: web 319/319, server 236/236, engine 2204/2204; all three package typechecks + `vite build` PASS; `git diff --check` clean; zero NUL/control bytes in authored files (only legitimate `—` em-dashes); four user-owned paths (`.gitignore`, `AGENTS.md`, `ChatPane.tsx`, `SlashPalette.tsx`) untouched. `fidelity-ledger.md` gained the dated Task-4 sub-table (10 comparison rows, all Match) + judgment. DevHub-OWN Browser QA (Playwright against the staged `evidence/m6/thread/fixture-{empty,sparse,active,complete}.html` — REAL `ThreadWorkspace` SSR markup in the shipped `index.css`, served on :5209, 1440x900): measured user fill `rgb(36,36,36)`=`#242424`/max 566/flex-end, assistant `rgba(0,0,0,0)`+no border/radius+`data-dh-unframed`, empty transcript 0 children/0 SVG/0 hero + composer still 98, request `rgb(38,38,38)`=`#262626` `role="group"` non-modal, composer 736x98/r21/16/`#2d2d2d` with `data-dh-send-state="stop"`, transcript 736, one polite live region, 0 logos — 13 comparison points all Match; only benign `@import` 404s, zero JS errors. Evidence: `evidence/m6/thread/{qa-note.md,m6-thread-wide.png,m6-thread-active-wide.png}` + the four fixtures. SCOPE (honest): this claims the workspace PRESENTATION, surface/unframed discipline, non-modal request rules, stable-composer geometry, and flag safety only; live mounting into the task canvas — mapping native events to `ThreadItem[]` — is a DEFERRED data-wire because the live transcript canvas is owned by the user-owned `ChatPane.tsx` (off-limits) and `CodexNativePane`, mirroring EXACTLY how Task 3 deferred its header data-wire; the flag gate + resolver land here and the App composition mount is staged for the Task 9 `codexStyleShell` cutover. NOT promoted to `campaign/auto-improve`; the M6 milestone stays [RONAK-GATE]; `nativeCodex`/`persistentClaude` stay false; `threadWorkspace` requested-default stays false. PRIOR — M6-HEADER-PROVIDER-SETUP (Extracted the canonical `TaskHeader` + provider-aware `TaskSetup` behind a NEW default-OFF `taskHeaderSetup` slice flag on `wip/devhub-background-runner` — M6 Task 3, the third strangler slice, implementing design-lock §3/§5. NEW `apps/web/src/components/features/providers/provider-capabilities.ts` is the single source of truth: `providerIdentity` (quiet `OpenAI · Codex`/`Anthropic · Claude`, never a logo), `PERMISSION_FIELD_LABEL` (Codex `Permissions` / Claude `Permission mode`, DERIVED from the provider so the not-interchangeable invariant can't be violated by bad data — Claude never renders `Workspace`), `decideSetupFields` (a pure capability gate: Provider/Project/Folder always shown; Model/Reasoning/Mode/permission SHOWN only when the runtime reports a non-empty inventory else ABSENT; reasoning is Codex-only; a schema-named-but-unproven control is emitted as `disabled` ONLY together with a non-empty `reason` so CSS state never substitutes for a missing runtime contract), `canCreateTask`/`createTaskDisabledReason` (Create gated on auth+project+folder+policy with a deterministic first-unmet accessible reason), and `resolveClaudeModelDisclosure` (diverges only when a known session-reported/response-used model differs from requested; surfaces the exact `Model differs from request` and keeps the three values distinct so the requested model is NEVER claimed to have run). NEW `TaskSetup.tsx` renders the compact anchored `New task setup` panel (not a wizard/hero): the fixed-provider disclosure `Provider is fixed after creation. Fork to another provider to continue there.`, a setup-time provider picker (setup-only), capability-gated field rows (disabled rows carry a REAL `disabled` attribute + `data-dh-capability-reason` wired via `aria-describedby`), and a `Create task` button disabled-with-accessible-reason until valid. NEW `TaskHeader.tsx` renders the 46-high existing-task header with a truncating title + QUIET read-only provider identity and NO in-task provider control (provider immutable; change is only `Create cross-provider fork`, source unchanged); for a Claude task ONLY, on divergence it shows Requested/Session reported/Response used each under its own label + the warning copy, and a Codex task computes no Claude disclosure (a permanent Claude warning beside a Codex task is rejected). Additive `.dh-task-header*`/`.dh-task-setup`/`.dh-setup-*`/`.dh-task-model-divergence` CSS added to `index.css` using existing `--dh-*` tokens only (no legacy-token mutation). The `taskHeaderSetup` flag was added (default false) to the engine `DevHubFeatureFlags` + default, the server settings-route `FEATURE_KEYS`/`completeFeatures`/`ALL_FEATURES_APPLIED`/JSON-schema, and the web `completeDevHubFeatures`; exact-shape flag literals updated in engine/server/web tests (`toHaveLength(8)`→`9`). TESTS: NEW `TaskSetup.test.ts` (16) — capability gating per provider, reasoning Codex-only, provider-native permission label (never `Workspace`), absent-vs-disabled-with-required-reason, Create gating + reasons, compact-not-hero, no logos, and the flag gate; NEW `TaskHeader.test.ts` (11) — quiet immutable identity, NO in-task provider picker/`<select>`, fork-only change, title truncation, no logos, Claude divergence copy present-only-on-divergence, Codex never shows Claude copy, and pure `resolveClaudeModelDisclosure`. FULL suites GREEN with the new field: web 289/289 (+27), server 236/236, engine 2204/2204; all three package typechecks + `vite build` PASS; `git diff --check` clean; no NUL/control bytes in authored files. DevHub-OWN Browser QA (Playwright against a self-contained `fixture.html` built from the SHIPPED `index.css` + REAL `TaskHeader`/`TaskSetup` SSR markup, served on :5209, 1000x780): live measurements — header 47 (46+1px border), title `ellipsis/nowrap/hidden`, identity is a `<span>` (0 header `<select>`, 0 `data-dh-provider-picker`), Codex reasoning present + Claude reasoning absent, Claude permission label `Permission mode`, Claude Create disabled with reason `Choose a project.`, divergence present with `Model differs from request`, 0 logos — 13 comparison points, all Match. Evidence: `evidence/m6/taskheader/{qa-note.md,fixture.html,m6-taskheader-wide.png}`; `fidelity-ledger.md` gained the Task-3 sub-table (13 rows) + judgment. SCOPE (honest): this claims the header/setup PRESENTATION, capability-gating, provider-immutability, and Claude divergence copy + flag safety only; live mounting into the task canvas is a later data-wire (the per-task canvas header lives in the user-owned `ChatPane.tsx`, off-limits to this slice), mirroring how Tasks 1–2 left slots/rows as later data-wires. NOT promoted to `campaign/auto-improve`; the M6 milestone stays [RONAK-GATE]; `nativeCodex`/`persistentClaude` stay false; `taskHeaderSetup` requested-default stays false. PRIOR — M6-T2-TASKRAIL (Extracted the canonical `TaskRail` as a Codex-style OPEN LIST behind a NEW default-OFF `taskRail` slice flag on `wip/devhub-background-runner` — M6 Task 2, the second strangler slice. NEW `apps/web/src/components/features/shell/TaskRail.tsx` renders the rail CONTENT (no own `<nav>` landmark — the shell/AppShell rail slot owns it) as flat `role="list"` task rows grouped under quiet section headings (design-lock §4 "Rail hierarchy is an open list"; NOT nested cards): each row carries provider identity as QUIET TEXT — a visible suffix (`Codex`/`Claude`) plus the full `OpenAI · Codex`/`Anthropic · Claude` in its accessible name, provider quiet-but-never-absent and NEVER a generated logo (the component renders zero `svg`/`img`); the selected row is the measured 256x30 compact fill (`#313131`, 9px radius) at an 8-unit rail inset and its height stays 30 when a quiet active spinner appears; roving focus (`nextRovingIndex` covers Arrow/J/K/Home/End/Enter) puts exactly ONE row open-button at `tabIndex 0` while the overflow `Actions for {task}` trigger is independently `tabIndex 0` and reachable without hover; a single provider's `failedProvider` marks ONLY that provider's rows (other-provider rows never marked failed); reachable secondary destinations render as an `aria-current`-aware pinned footer and unreachable ones are ABSENT (never `disabled`/`aria-disabled`); the empty state is `No tasks` and the action copy is `New task` (`T-rail`); `sanitizeRailKey` strips NUL/control chars + path separators so no raw home (`/Users/…/.codex/…`) or NUL ever reaches a rendered key/attribute; brand stays `DevHub`. Additive `--dh-rail-inset`/`--dh-selected-row-width`/`--dh-selected-row-height` tokens + the `.dh-tasklist*`/`.dh-task-*`/`.dh-rail-dest*`/`.dh-sr-only` CSS (reduced-motion-aware spinner) were added to `index.css` WITHOUT mutating legacy tokens; one QA-found layout fix landed — `.dh-tasklist-root { height:100% }` + list `flex:1 1 auto` so the flex-basis-0 list did not collapse to zero inside the block-level rail-content. The `taskRail` flag was added (default false) to the engine `DevHubFeatureFlags` + default, the server settings-route `FEATURE_KEYS`/`completeFeatures`/`ALL_FEATURES_APPLIED`/JSON-schema, and the web `completeDevHubFeatures`; `App.tsx` computes `resolveTaskRailMode(settings)` and the rail slot renders `<TaskRail>` (with a memoized model built from the reachable primary tabs as secondary destinations + an empty task list) ONLY in the `devhub` branch, so flag-OFF neither builds the model nor instantiates TaskRail and keeps the legacy `ProjectsPane`/`SessionsPane`/`RecentMenu` rail byte-for-byte. TESTS: NEW `TaskRail.test.ts` (20) — open-list semantics, selected-row 256x30/inset/fill/geometry-frozen (mirrors `SHELL_GEOMETRY`), height-invariant active spinner, single roving tabstop, provider quiet suffix + full accessible identity, no logos, overflow reachable/independently tabbable, reachable-only never-inert destinations, single-provider failure isolation, NUL/raw-home-free keys, `nextRovingIndex` math, and the `taskRail`/`isTaskRailApplied` gate; updated the exact-shape flag literals in engine feature-flags, server settings-features, and web App/SettingsPane native-codex tests (`toHaveLength(7)`→`8`) for the new field. FULL suites GREEN with the new field: web 262/262 (+20), server 236/236, engine 2204/2204; all three package typechecks + `vite build` PASS; no NUL/control bytes in any authored file (verified with `tr -cd`); dist untracked; `git diff --check` clean. DevHub-OWN Browser QA (Playwright against a self-contained `fixture.html` built from the SHIPPED `index.css` tokens/`.dh-*` blocks + the REAL `TaskRail` SSR markup, mounted in the shipped `.dh-rail`/`.dh-rail-content` chrome, served on :5202, 1000x900 `device` scale): live `getBoundingClientRect`/`getComputedStyle` measured rail=274 (273+1px border), selected row 256x30 at inset 8, fill `rgb(49,49,49)`=`#313131`, radius 9px, quiet suffixes `Codex`/`Claude`/`Codex`, active spinner present with height still 30, 1 roving `tabIndex 0`, all overflow actions `tabIndex 0`, 0 logos, `role="list"` with 3 rows all visible (not clipped), destinations pinned at bottom, empty state `No tasks` — 16 comparison points, all Match. Evidence: `evidence/m6/taskrail/{qa-note.md,fixture.html,fixture-empty.html,m6-taskrail-wide.png,m6-taskrail-empty.png}`; `fidelity-ledger.md` gained the `## M6` Task-2 sub-table (12 rows) + judgment. SCOPE (honest): this claims the rail OPEN-LIST PRESENTATION, provider quiet-text identity, selection geometry, roving/overflow/failure-isolation semantics, and flag safety only; the App model currently feeds reachable primary tabs as secondary destinations + an EMPTY task list, so populating live native task rows (each with its immutable provider identity) is a later data-wire deliberately NOT in this unit (mirroring how Task 1 left rail rows as slots). NOT promoted to `campaign/auto-improve`; the M6 milestone stays [RONAK-GATE]; `nativeCodex`/`persistentClaude` stay false; `taskRail` requested-default stays false. PRIOR — M6-T1-SHELL-GEOMETRY (Built `DevHubShell` + application chrome behind a NEW default-OFF `shellChrome` slice flag on `wip/devhub-background-runner` — M6 Task 1, the first strangler slice. NEW `apps/web/src/components/features/shell/DevHubShell.tsx` renders the locked container/geometry from `design-lock.md` §4 using `design-system.md` tokens only: a 273-unit rail (with the `DevHub` wordmark), a 46-unit header, an open `#181818` canvas with no page card, a shared 736-unit transcript+composer column (composer 736x98 slot + 16 bottom gutter, ~21 radius), and a 300-unit content-height inspector dock in a 316-unit lane (12 top / 16 right gutter, 16 radius). It owns the shell landmarks (one `main`, one named rail `nav`, an optional named `complementary` inspector) + a skip link, isolates a provider/region failure via a `RegionBoundary` around the canvas/inspector slots, keeps geometry invariant across rest/loading/streaming (only `aria-busy` differs), and collapses responsively on ONE rendered rail (no hidden duplicate tabbable). Geometry is exported as a frozen `SHELL_GEOMETRY` constant and read from `--dh-*` tokens (no invented literals in the tree). Additive `--dh-*` dark palette (§2.1) + wide-reference geometry (§3.1) + radius scale (§4.2) + the `.dh-*` frame CSS (with 1439/1023/767 media queries) were added to `apps/web/src/index.css` WITHOUT mutating the legacy `--bg`/`--panel`/`--accent` tokens (the shadcn semantic migration stays a separate task). NEW `apps/web/src/components/features/shell/AppShell.tsx` is the composition seam: `mode==="legacy"` reproduces the current chrome byte-for-byte (skip link, TopBar slot, ErrorBoundary, `#main-content`, the w-44 primary-navigation rail) and returns a fragment so App's outer wrapper + overlays stay put; `mode==="devhub"` mounts `DevHubShell`. `App.tsx` was reduced toward composition: the TopBar/rail-content/main-panes are now slots passed to `<AppShell mode={resolveShellChromeMode(settings)}>`; DevHubShell is instantiated ONLY in the devhub branch, so flag-off never builds the new tree. The `shellChrome` flag was added (default false) to the engine `DevHubFeatureFlags` + default object, the server settings-route `FEATURE_KEYS`/`completeFeatures`/JSON-schema/`ALL_FEATURES_APPLIED`, and the web `completeDevHubFeatures`; all additive and clamped like the M5 flags. TESTS (RED-first geometry/snapshot + preservation): NEW `DevHubShell.test.ts` (14) asserts the measured dimensions against `SHELL_GEOMETRY` + rendered `data-dh-*`, token-only markup (no hex/px leak), single main/rail/skip landmarks, exact `DevHub` brand (no provider wordmark), geometry-invariant-across-status, `RegionBoundary` isolation, and the `shellChrome` gate; NEW `AppShell.test.ts` (4) proves flag-OFF renders the current chrome unchanged AND does not instantiate DevHubShell, flag-ON mounts DevHubShell + drops legacy markers. Updated exact-shape flag literals in engine feature-flags, server settings-features, and web App/SettingsPane native-codex tests for the new field. FULL suites GREEN: web 242/242 (16 files, +18), server 236/236, engine 2204/2204; all three package typechecks + `vite build` PASS; `git diff --check` clean; no NUL bytes in authored files; four user-owned preservation paths untouched. DevHub-OWN Browser QA (Playwright against a self-contained `fixture.html` with the shipped tokens+DOM, served on :5199): live `getBoundingClientRect`/`getComputedStyle` at 1800x1130 measured rail=273, header=46, canvas=`rgb(24,24,24)`=`#181818`, transcript=736, dock=300/lane=316/top=12/right=16/radius=16, brand=`DevHub`, no h-overflow — 11 concrete comparison points, all Match (composer x=511 vs manifest 510 within ≤1px centering; brand `DevHub` an intentional deviation from the capture's `Codex`); narrow 768 (rail→48 icon strip, inspector hidden, 1 nav/main/skip, no overflow) and PWA 390 (rail display:none, header 44, no overflow) captured. Evidence: `evidence/m6/shell/{qa-note.md,fixture.html,m6-shell-wide-1800x1130.png,m6-shell-narrow-768.png,m6-shell-pwa-390.png}`; `fidelity-ledger.md` gained the `## M6` section + the Task-1 sub-table (M3-table shape, 12 rows). SCOPE (honest): this task claims the shell FRAME geometry, tokens, landmarks, and flag safety only — rail rows, header title/actions, transcript renderers, composer controls, and inspector destinations remain placeholder slots owned by M6 Tasks 2–8. NOT promoted to `campaign/auto-improve`; the M6 milestone stays [RONAK-GATE]; `nativeCodex`/`persistentClaude` stay false; `shellChrome` requested-default stays false. PRIOR — M6-IMPLEMENTATION-PLAN (Authored `.planning/devhub-codex-parity/m6-implementation-plan.md` — the M6 implementation plan, mirroring the m5 plan structure. It decomposes `design-lock.md` section 2 into the locked strangler slice order — (1) DevHubShell+chrome, (2) TaskRail, (3) TaskHeader+provider-aware setup, (4) ThreadWorkspace+ActivityTimeline, (5) stable Composer, (6) InspectorDock, (7) Search/command palette, (8) Settings/secondary — plus a Task 9 App.tsx composition-reduction + `codexStyleShell` cutover. Each slice carries: a RED-first testable DoD; its governing reference capture (REF-EMPTY/RICH/SPARSE/ACTIVE/SEARCH) + concept (C01/C02/C03/C04/C05/C08); the exact `component-state-matrix.md` section + `surface-inventory.md` SF-/RT-/T- owners it maps to; its own default-OFF slice feature flag (`shellChrome`/`taskRail`/`taskHeaderSetup`/`threadWorkspace`/`composerSurface`/`inspectorDock`/`searchCommands`/`settingsSecondary`, additive to `DevHubFeatureFlags`, with the umbrella `codexStyleShell` as the final requested-default cutover); a required `fidelity-ledger.md` M6 entry in the exact M3-table shape; and per-slice gate criteria (current/min/narrow screenshots, >=5 comparison points, visible-copy diff, keyboard/a11y, F/P/R/S/V). Lists the M6 frozen invariants: provider immutable after creation; capability-gated controls never faked by CSS; preserved surfaces never disappear; no dashboard/marketing chrome in the task canvas; measured geometry is authority; concept screenshots are never shipped UI; shadcn official-registry-only + audited custom preset; transcript never wrapped in ScrollArea; brand always DevHub; dark reference is authority; keyboard/a11y/reduced-motion mandatory; each slice default-OFF with non-destructive rollback. DOC-ONLY change: validated no NUL/control bytes, all referenced planning files + 5 reference captures + 8 concepts resolve; 244 lines. `git diff --check` clean; four user-owned preservation paths untouched. No code/flags changed — `nativeCodex`/`persistentClaude` stay false. Committed on `wip/devhub-background-runner`; NOT promoted; the M6 milestone itself stays [RONAK-GATE]. PRIOR — M5-WIRE-LOCATOR-TRANSPORT (Consumed the previously-untestable `unifiedTaskIndex` locator transport seam in the web UI — the deferred M5-T7 scope-note item. The provider task pane entry point `CodexNativePane` is now a thin flag gate: `isUnifiedTaskIndexApplied(features)` true → renders the NEW `apps/web/src/components/ProviderHomeSetup.tsx`, a PublicProviderHome-only setup that calls `selectProviderTransport(features)` and discovers homes EXCLUSIVELY through the path-free `provider-index-api` facade `homes()` (returns `{provider,homeFingerprint,status,capabilities}` — never a raw filesystem home, so the view can neither render nor request one); false/undefined → renders the existing `CodexNativeDirectPane` (the unchanged monolith, now just renamed) which keeps the direct key-based `providerApi` routes byte-for-byte as the instant rollback surface. The branch is a component SWAP so each side owns its own hooks and no state leaks across a flag flip. `App.tsx` passes `features={settings?.devHubFeatures}` to both pane mounts. CRITICAL BUILD FIX: consuming the facade dragged its ONLY runtime engine import — `serializeTaskLocator` from `@devhub/engine/providers`, which transitively pulls `node:fs`/`realpathSync` via `task-key.ts` — into the browser graph and broke `vite build` (the exact eager-bundle hazard M5-T7 flagged for router.ts). Fixed with the same regex-mirror pattern: replaced it with a browser-safe local `serializeTaskLocator` in `provider-index-api.ts` reproducing the exact `pt1.<provider>.<64-hex fingerprint>.<base64url(nativeTaskId)>` grammar using only Web APIs (`TextEncoder`+`btoa`), path-free by construction; a drift-guard test asserts byte-identical output vs the real Node engine serializer. Focused tests: NEW `apps/web/src/components/ProviderHomeSetup.test.ts` (12) — transport selection by flag state (undefined/{}/false → direct fallback rendered, never the indexed setup; true → indexed loading shell, discovery not fired under static render), `discoverProviderHomes` consumes only `homes()` + preselects preferred-else-first + object has no `home` field + no `/`, real-facade `homes()` request is GET/no-body/no-raw-home, ProviderHomePicker DOM is fingerprint+capabilities only with ZERO raw-home markers and ZERO NUL, single-home no-`<select>` path, pure home helpers, and the flag gate proving flag-off wrapper markup === `CodexNativeDirectPane` markup byte-for-byte (both "Checking native Codex runtime", neither "Discovering native") while flag-on swaps to the PublicProviderHome-only setup; PLUS +1 in `provider-index-api.test.ts` (7→8) grammar-drift guard. FULL web suite 224/224 (14 files, was 211/211), web `tsc --noEmit` PASS, `vite build` PASS (proves the eager bundle stays Node-free with the facade now consumed). `git diff --check` clean; no NUL/control bytes in any authored file; raw-home scan clean (the only `node:fs`/`.codex` hits are the explanatory comment + test fixtures). SCOPE (honest): flag-ON renders the setup surface only — the full indexed task LIST/READ/SEND/etc. operations over locators are a larger migration deliberately NOT in this unit; and since `nativeCodex`/`persistentClaude` stay FALSE the combined flag-ON pane never mounts live, so nothing regresses in the shipping default. Committed on `wip/devhub-background-runner`; NOT promoted; `nativeCodex`/`persistentClaude` stay false. PRIOR — M5-FINALIZE-CUTOVER (Integrated the reviewed `unifiedTaskIndex` cutover from `staging/m5-t9-cutover@01f39e4` ONTO `wip/devhub-background-runner` — `DEFAULT_DEVHUB_FEATURE_FLAGS.unifiedTaskIndex` false->true + `app.ts` applied-truth + the 4 focused tests, applied clean over the docs tip `6631cef`. FULL heavy-queue gate with CACHE BYPASS all GREEN: engine `vitest run --no-cache` 2204/2204 (79 files), server 236/236 (11 files), web 211/211 (13 files); all 5 package typechecks via `turbo run typecheck --force` PASS; `turbo run build ...--force` (fresh vite build, no cache) PASS for engine+server+web. One full-suite fallout the focused staging run never exercised was fixed in this commit: `gate2-drills.test.ts` had hard-coded the pre-cutover default-false with a "cutover has not happened" comment; updated to assert the now-true default while still proving explicit stored-false wins (its actual intent). Explicit stored-false ROLLBACK re-proven in `provider-index-composition.test.ts` (3/3): default-on inits the coordinator once at ready + reports the feature applied; an explicit stored `unifiedTaskIndex:false` persisted before ready => no coordinator built + feature reported disabled; re-enable builds once; repeat/flip-back never rebuilds or tears down. DevHub-OWN Browser QA done against the BUILT bundle served locally (static+proxy shim on :5191 -> tsx server on :8791), fully ISOLATED to a scratch `$HOME`/`CLAUDE_CONFIG_DIR`/`DEVHUB_DATA` with one 667-byte synthetic Claude session (real 9.2G+8.2G corpora never scanned; no dataset writes): end-to-end `GET /api/settings` resolves `devHubFeatures.unifiedTaskIndex=true` (server reports resolved-true only when the shared store exists AND the coordinator initialized), `nativeCodex`/`persistentClaude` stay false; Home/Browse/Settings each rendered with 0 console errors (only a benign pre-existing `apple-mobile-web-app-capable` PWA meta deprecation warning). Evidence under `evidence/m5/`: `qa-note.md`, `qa-live-settings.json`, `qa-screenshots/{01-home,02-browse,03-settings}.png`. `git diff --check` clean; four user-owned preservation paths untouched (hashes re-verified: .gitignore 03479b6, AGENTS.md 28a7929, ChatPane.tsx cfecb54, SlashPalette.tsx 737110f). This lands the cutover on the wip branch ONLY; PRODUCTION release default-on + `origin/main` merge remain the held [RONAK-GATE] human actions; `nativeCodex`/`persistentClaude` stay false. PRIOR — M5-T9-STAGE-CUTOVER (Final independent M5 reviews + a STAGED `unifiedTaskIndex` cutover prepared on an ISOLATED branch, with the default-on flip held as the single [RONAK-GATE] human action. FRESH SPEC REVIEW: GO — every M5 frozen invariant (1-12) and every Task 1-9 contract maps to a landed, tested surface (identity/home-fingerprint path-free `pt1.` locator + 64-hex fingerprint; schema v14 base + v15 generation-epoch repair; store/cursor/coordinator readThrough+rebuild; writer-lease-fenced adapter mutations with the single shared `NativeTaskWriterLeaseStore`; locator-only authenticated routes + SSE; archive v2 authority separation; web `devhub:*` compat + path-free `providerTask` URL; env/webhook aliases; gate-2 drills 911/911); NO missing requirement. FRESH ADVERSARIAL REVIEW: GO — no open P1/P2; the two prior T7 P3 nits stay accepted (doc-comment + documented non-security FNV token); ONE new P3 on cutover honesty (settings could report `unifiedTaskIndex` applied while a fail-open coordinator init left it null) was FIXED in the cutover commit by tying `appliedDevHubFeatures.unifiedTaskIndex` to `coordinator !== null`; no unaccepted P3 remains. GATE-5 CHECKS all pass (`evidence/m5/precutover-checks.md`): `git diff --check` clean; targeted secret scan clean in non-test source + evidence (only synthetic redaction fixtures in tests); raw-home + NUL scan of evidence artifacts clean; generated-file check clean (no tracked dist/build/emitted js); preservation hash/status check — four user-owned paths untouched (blob hashes recorded), preservation matrix unchanged. STAGED CUTOVER: isolated branch `staging/m5-t9-cutover` commit `01f39e4` (based on reviewed tip `f4d27fd`; NOT `campaign/auto-improve`, NOT `origin/main`, NOT `wip/devhub-background-runner`) flips `DEFAULT_DEVHUB_FEATURE_FLAGS.unifiedTaskIndex` false->true (6 files: feature-flags.ts + app.ts applied-truth + 4 focused tests). Semantics: requested default ON, resolved value clamped to `requested && available(store exists) && applied(coordinator initialized)`; explicit stored `unifiedTaskIndex:false` wins over the default (non-destructive rollback, no down-migration, legacy routes byte-compatible). FOCUSED flag/settings/routes/rollback tests rerun GREEN on `01f39e4` at `--maxWorkers=2`: engine feature-flags+devhub-feature-settings 4/4, server settings-features 8/8 + provider-index 25/25 + provider-index-composition 3/3 = 36/36 (40 focused total), plus app.test.ts 80/80 unaffected; engine+server+public-surface tsc PASS; `git diff --check` clean; owned paths untouched. Explicit stored-false ROLLBACK rehearsed in provider-index-composition.test.ts (default-on inits coordinator once at ready + reports applied; explicit stored-false -> no coordinator + feature reported disabled; re-enable builds once; repeat/flip-back never rebuilds). Evidence staged under `evidence/m5/`: `cutover-staged.SUMMARY.md`, `precutover-checks.md`, `cutover-{engine-flags,server-routes}.{json,log}`. REMAINING single human action = promote `01f39e4` + run the full exact-tip heavy-queue engine/server/web gate with forced cache bypass + capture Browser/Computer-Use QA evidence under `evidence/m5/`. `nativeCodex=false`/`persistentClaude=false` stay (separate billable live-proof gates). This task changed only `tasks/STATUS.md` + `evidence/m5/` on `wip/devhub-background-runner`; the flag itself stays false on wip — the flip lives only on the staging branch.). PRIOR — M5-T9-DRILLS (Task 9 gate-2 induced-failure drill suite landed on `wip/devhub-background-runner`, NOT promoted: new consolidated `packages/engine/test/provider-index/gate2-drills.test.ts` (10 tests) reproduces the cross-cutting gate-2 properties directly against real engine APIs — v13 migration recovery/idempotent-rerun-after-reset-to-13, flag-rollback no-schema-down-migration + explicit-stored-false-wins, DB-busy fail-closed + recovery, invalid/unknown-fingerprint locator rejection, cursor scope abuse, path-free fingerprint; the remaining gate-2 items are covered by the existing RED-first adversarial suites, each mapped item->test in `evidence/m5/gate2-drills.manifest.json`. New drill runner `evidence/m5/run-gate2-drills.sh` executes the whole corpus at <=2 workers (`--maxWorkers=2`) with NO provider process spawned (runtimes never enabled): engine 806/806 (18 files) + server 105/105 (2 files) = 911/911 green, staged as `evidence/m5/{engine,server}-drills.{json,log}` + `gate2-drills.SUMMARY.md`. engine `tsc --noEmit` + provider-index public-surface tsc PASS; `git diff --check` clean; only the one new test file + `evidence/` added. Flags unchanged — `nativeCodex`/`persistentClaude`/`unifiedTaskIndex` stay false. Cutover (feature-flags default flip) is deliberately NOT done: it is a hard RONAK-GATE.). PRIOR — M5-T7-REVIEW (independent SPEC + QUALITY/SECURITY review of the built M5-T7 web-compat slice: compat-storage DevHub-first read → exact legacy fallback → copy-forward-without-delete/rewrite, DevHub-only write, remove-both on logout, SSR+quota+private-mode+getter-throw non-fatal, legacyKeyFor known-keys-only; router providerTask=<serialized locator> parse/build/stable-order/malformed-drop/NUL-drop with legacy URLs unchanged and grammar confirmed against the real engine serializer; provider-owned drafts/identity/dedup keys path-free + NUL-free via pathFreeHomeToken; eager App bundle Node-free proven by a clean vite build; zero raw-home/NUL leak in authored source; re-ran web focused 93/93, FULL web 211/211, tsc PASS, vite build PASS, git diff --check + tree clean; VERDICT GO, no P1/P2, two non-blocking P3 nits, no tracked fix task; M5-T7 gate record recorded below; not promoted). PRIOR — M5-T5-REVIEW (independent SPEC + QUALITY/SECURITY review of the built M5-T5 indexed-routes slice: full route matrix / all auth modes with no query-string tokens / locator-only path-free DTOs / list order + opaque pi1. scope-bound cursor with scope-change rejection / freshness read-through + 404 + reconciliation / SSE subscribe-buffer-snapshot-drain-live with resync-required + Last-Event-ID full path / reconciliation get+ack CAS / additive meta / single-flight rebuild 409 / flag-off byte-compatibility ALL confirmed; recursive raw-home/NUL scan across every success/error/log/SSE surface clean; server provider-index 25/25, full server 236/236, web facade 7/7, server+web typecheck PASS, git diff --check clean; VERDICT GO, no P1/P2/unaccepted P3, no tracked fix task; M5-T5 gate record recorded below; not promoted). PRIOR — M5-T8-ALIASES (new packages/engine/src/compat-identifiers.ts: DEVHUB_* preferred / exact CLAUDE_UI_* alias / conflict→DevHub-value + value-free diagnostic; DEVHUB_DATA+CLAUDE_UI_DATA via one resolver keeping legacy ~/.claude-ui default (M8 owns migration); paths.appDataDir + server buildApp token routed through it; child-provider scrub now shares isDevHubNamespaceKey; webhooks payloadVersion 1|2 — v1 byte-compatible source:"claude-ui", v2 source:"devhub"+schemaVersion:2+optional sourceAliases, buildWebhookPayloadFor emits exactly one version. engine 2194/2194 + server 236/236 green; engine+server typecheck + git diff --check clean; DONE, not promoted)
Goal: Deliver the mandatory M0-M8 DevHub goal as a provider-native Codex-quality environment for real Codex and Claude runtimes, with preservation, review, verification, and conditional gates reported honestly.

Pause authority: Ronak explicitly paused this goal for migration to a background runner. There are zero autonomous next-actions. No agent may resume a test, review, implementation, promotion, or cutover until Ronak explicitly resumes the goal and names the execution target.

Progress: 3 / 9 milestones done (33%) — denominator FROZEN at 9. Do not change it; partial M3-M8 work receives no percentage credit.

Blocker legend:
  [SOFTWARE]    = unblocked, any agent can start now
  [ALEX]        = waiting on Alex (external human)
  [HARDWARE]    = waiting on a physical device / bench test
  [S3]          = waiting on data/asset in S3
  [RONAK-GATE]  = needs Ronak's decision or approval before proceeding

## Core tasks (counts toward %)
- [x] M0  Baseline, backup, preservation, and execution plan                 [SOFTWARE]   → RESOLVER: `.planning/devhub-codex-parity/baseline.md`
- [x] M1  Native provider contracts, reference package, and design approval  [SOFTWARE]   → RESOLVER: `.planning/devhub-codex-parity/provider-spike-results.md`
- [x] M2  Provider-neutral adapter seam and disabled-by-default flags         [SOFTWARE]   → RESOLVER: `.planning/devhub-codex-parity/implementation-plan.md#M2---Provider-adapter-seam`
- [ ] M3  Native Codex vertical slice, including production-wrapper resume    [SOFTWARE]   → RESOLVER: `.planning/devhub-codex-parity/evidence/m3/live-runtime-resume-proof.md` (RESUME PROOF CAPTURED 2026-07-15: real billable production-wrapper resume+continued-conversation PASS; sole remaining M3 live blocker CLEARED. `nativeCodex` requested-default flipped ON 2026-07-15 per Ronak — see DEVHUB-FLIPS below; server still clamps to real runtime availability.)
- [x] M4  Persistent Claude vertical slice, including raw lifecycle parity    [SOFTWARE]   → RESOLVER: `.planning/devhub-codex-parity/evidence/m4/lifecycle-proof-rerun-2026-07-15-keyfile.md` (M4 DONE 2026-07-15 — DEVHUB-M4-PROOFS: with the handshake fix in place, all SIX raw-lifecycle proofs (multi-query / resume / permission / interrupt / post-interrupt / fork) PASSED 6/6 live against the EXACT staged 2.1.207 arm64 binary with the scoped `~/.config/6thsense/devhub-agent.env` key. `start()` now resolves on the initialize handshake in ~1.15s (was 5008ms `INIT_TIMEOUT`). Adversarial security review CLEAN: auth (`requireClaudeProgrammaticAuth`) still gates `acquire()` pre-spawn; session-identity is DEFERRED to the turn-1 `system/init`, NOT skipped (mismatch throws `MALFORMED_FRAME` before any turn-1 content is delivered, terminating the process). No auth bypass, api-key requirement intact. `persistentClaude` requested-default flipped ON; `NativeClaudeLifecycleEvidence` wired into `createNativeClaudeRuntime` in `app.ts`; server still clamps to real runtime availability (compatible install + programmatic auth + mutation token). Prior blocker history preserved below. DEVHUB-M4-KEY 2026-07-15: CREDENTIAL GATE CLEARED — key IS retrievable non-interactively from keychain via `security find-generic-password -a "env:local:shared:ANTHROPIC_API_KEY" -w` and the runtime constructs with `auth.method="api-key"`. But the six proofs STILL did not run: a NEW credential-independent blocker surfaced — `ClaudeCliProcess.start()` rejects after 5004ms with `INIT_TIMEOUT`. Root cause: readiness gates on a pre-turn `system/init` envelope, but CLI 2.1.207 in stream-json mode only emits `system/init` DURING the first user turn (verified empirically 3 ways + control_response handshake), so start() deadlocks before any lifecycle turn. Fix = readiness-handshake change in `cli-process.ts` (use the SDK `initialize` control_response as ready-signal; validate session identity when `init` arrives on turn 1) — SECURITY-ADJACENT, needs Ronak's explicit go, NOT slipped into a background run. `persistentClaude` stays false, no `lifecycleEvidence` wired, NO fabricated evidence. Six proofs + evidence wiring still to do AFTER the handshake fix. DEVHUB-M4-KEYFILE 2026-07-15 RERUN with the scoped `~/.config/6thsense/devhub-agent.env` key CONFIRMS this — proofs passed 0/6; auth re-cleared (`method="api-key"`, real "ok" turn); the `INIT_TIMEOUT` reproduced freshly against BOTH the production `ClaudePersistentSupervisor.acquire()` path (rejected 5030ms `UNAVAILABLE` "Claude runtime startup failed", 0 events) AND raw `ClaudeCliProcess.start()` (rejected 5008ms `INIT_TIMEOUT`, ZERO pre-turn envelopes); the recommended fix is viable — a raw SDK `control_request{initialize}` returned `control_response` in 1418ms with NO pre-turn `system/init` in 8s. Doc-only change, no source touched; empirical repro IS the verification; key never printed/logged/written/committed. See `.planning/devhub-codex-parity/evidence/m4/lifecycle-proof-rerun-2026-07-15-keyfile.md`. M4-HANDSHAKE-FIX 2026-07-15 LANDED on `wip/devhub-background-runner`: the recommended fix above is now implemented — `ClaudeCliProcess.start()` resolves on the `control_request{initialize}`→`control_response` handshake instead of pre-turn `system/init`; session-identity validation moved to the turn-1 `system/init` check (mismatch now fails/latches that turn, not startup). `cli-process.ts` + its test suite only (41→48 tests, all green); NO flag flips, NO `lifecycleEvidence` wiring. The six M4 lifecycle proofs + evidence wiring still have not run — that is the next M4 step, now unblocked.)
- [x] M5  Unified provider-locked task/index model                            [SOFTWARE]   → RESOLVER: `.planning/devhub-codex-parity/m5-implementation-plan.md` (M5 DONE on wip 2026-07-16 — M5-CUTOVER-FINALIZE: full exact-tip heavy engine/server/web gate re-run with forced cache bypass GREEN (engine 2206/2206, server 237/237, web 487/487; typechecks + `vite build` + `git diff --check` all clean — see `evidence/m5/gate-report.md`); new passing test proves an explicit stored `unifiedTaskIndex:false` still returns byte-identical legacy-route responses (`provider-index.test.ts`); fresh DevHub-own Browser QA of the indexed-transport UI with the flag applied-on captured at two breakpoints with real screenshots + a live in-page `/api/provider-index/*` round trip (`evidence/m5/browser-qa.md`). Reconciled `staging/m5-t9-cutover@01f39e4` against wip: wip's flip supersedes it (independently reached the same flag state with a superset of verification); no fast-forward needed. `origin/main` merge and the first-party `com.openai.codex` Computer-Use QA remain HELD [RONAK-GATE]/hard gate, per scope.)
- [x] M6  Approved reference-first Codex-style shell                          [SOFTWARE]   → RESOLVER: `.planning/devhub-codex-parity/m6-implementation-plan.md` (implementation plan AUTHORED 2026-07-15; ALL nine slices — shellChrome/taskRail/taskHeaderSetup/threadWorkspace/composerSurface/inspectorDock/searchCommands/settingsSecondary + the umbrella `codexStyleShell` wiring — built+tested behind default-OFF flags on `wip/devhub-background-runner` (see M6-T1..T9 Blocked entries below); M6-CODEXSTYLESHELL-CUTOVER 2026-07-16 flipped the umbrella + all 8 slice flags to requested-default-true, per Ronak's software-implementation authorization — see that entry above and `evidence/m6/cutover/`. `origin/main` merge / `campaign/auto-improve` promotion stay held per scope.)
- [ ] M7  Cross-provider fork, DevHub Work mode, and synchronization           [RONAK-GATE] → RESOLVER: `.planning/devhub-codex-parity/implementation-plan.md#M7---Cross-provider-fork-Work-mode-synchronization` (PAUSED: explicit resume required)
- [ ] M8  Packaging, cutover, performance, preservation, and cleanup           [RONAK-GATE] → RESOLVER: `.planning/devhub-codex-parity/implementation-plan.md#M8---Desktop-packaging-cutover-performance-cleanup` (PAUSED: explicit resume required)

## Blocked — needs a human (agents STOP here, do not loop)
- GOAL [RONAK-GATE] Ronak must explicitly resume the DevHub goal and identify the background runner/checkout that owns execution. Until then, every remaining item below is paused and no autonomous action is authorized.
- M3  [RESOLVED 2026-07-15] Billable turns were authorized and the production-wrapper resume + continued-conversation proof PASSED with real captured evidence (`evidence/m3/live-runtime-resume-proof.md`). No longer blocked. `nativeCodex` requested-DEFAULT flipped true 2026-07-15 (Ronak-directed; see DEVHUB-FLIPS below), clamped server-side to real runtime availability.
- M4  [SOFTWARE] Billable turns authorized; not a human gate anymore. Remaining WORK (any agent): drive the six raw-lifecycle proofs (multi-query/resume/permission/interrupt/post-interrupt/fork) directly against `ClaudeNativeAdapter`+`ClaudePersistentSupervisor` using the staged exact `2.1.207` arm64 binary via `DEVHUB_CLAUDE_EXECUTABLE`, then wire the EARNED `lifecycleEvidence` object into `createNativeClaudeRuntime` in `app.ts`. Until all six really pass, keep `persistentClaude=false` and do NOT fabricate the evidence (`evidence/m4/live-gate-status-2026-07-15.md`).
- M5-T3A [RONAK-GATE] After explicit resume only, the exact next code unit is RED-first bounded FIFO per-locator lanes, operation reservations, observation epochs, `observeListPage`, and `observeTask` in the clean `m5-task3-lanes` worktree based on tested `cdbff7d`. No tests or implementation for this unit have started.
- M5-T3B [DONE 2026-07-15] Shared native->indexed projector + coordinator `readThrough`/degraded-cache/verified-legacy routing landed RED-first on `wip/devhub-background-runner`. 605/605 provider-index tests (16 new in `coordinator-readthrough.test.ts`), full engine 2099/2099, typecheck + public-surface + `git diff --check` clean. Independent SPEC + QUALITY/SECURITY GO recorded below. Flags unchanged (`nativeCodex`/`persistentClaude`/`unifiedTaskIndex` false). NOT promoted to `campaign/auto-improve`.
- M5-T3C [DONE 2026-07-15] Bounded `coordinator.rebuild({provider,home,signal?})` landed RED-first on `wip/devhub-background-runner`: `beginStage` -> paginate native list (`includeArchived:true`, `limit=pageSize`) honoring defaults + hard caps, reject oversized pages/non-canonical (1..4096 UTF-8) / exact-repeat cursors, dedupe locators (winner = greatest non-null revision `updatedAt`, tie -> later page/item), point-read every final locator `includeTurns:true` in serialized-locator order via `stageSnapshot` only, pass exact five counts (`taskCount=snapshotCount=receiptCount=unique`, turn/event totals recomputed) to `promoteStage` with `providerVersion=null`. Synchronous non-overlapping heartbeat (cadence `max(250, floor(stageLeaseMs/3))`) via injected timers before/after each provider call and before each stage write/promotion; injected `now` is sole monotonic deadline authority (throw/regress/invalid -> CLOCK_FAILURE). Pre-abort -> CANCELLED before `beginStage`; mid-run cancel/deadline invalidates a run token, drains the heartbeat, aborts the exact stage, and reports cleanup/stage-loss ahead of CANCELLED/REBUILD_TIMEOUT. Per-locator observation epochs pinned + rechecked before promotion (newer read -> STALE_OBSERVATION). Single-flight REBUILD_BUSY; observation reservations released on all terminal paths; zero provider mutation calls; failed/aborted rebuilds `abortStage` and retain the prior active generation. New `coordinator-rebuild.test.ts` (23 tests). Changed files: `provider-index/coordinator.ts` + the new test only. 628/628 provider-index, 2122/2122 engine, typecheck + public-surface + `git diff --check` clean. NOT promoted to `campaign/auto-improve`; flags unchanged.
- M5-T3D [DONE 2026-07-15] Task 3 completion + promotion landed on `wip/devhub-background-runner` and promoted to `campaign/auto-improve`. Server composition (`packages/server/src/app.ts`) now creates + initializes the coordinator LAZILY on each false->true effective `unifiedTaskIndex` transition (and at `onReady` if already effectively on), never eagerly: it builds a frozen `registeredHomes` `{provider,home}` array from the same trusted native-runtime installs used to construct the adapters (Codex->`openai`, Claude->`anthropic`; no new raw-home accessor), passes it to `createProviderTaskIndexCoordinator({registry, store: engine.index.providerIndex, registeredHomes, clock})`, and calls `initialize()`. The engine factory already rejects extras/dupes/exotic, sorts by provider then raw-home ASCII, samples one `registeredAt`, calls `store.registerHome` per home, and returns frozen path-free `{provider,homeFingerprint,registeredAt}`. `TranscriptIndex` keeps sole ownership of the shared `ProviderTaskIndexStore` and never builds a coordinator; `unifiedTaskIndex` was made server-available so the flag can transition, but its requested default stays false so flag-off routing never instantiates or calls the coordinator (proved by test). The providers barrel remains path-free (no store/configured-home input/raw-home carrier/stage-lease/projector); the root engine exports the concrete coordinator/factory. Server `NATIVE_TASK_MISSING`->HTTP 404 `{error:'provider_task_not_found',code:'NATIVE_TASK_MISSING',provider}` and the web safe-code/public-tag allowlist retention (`NATIVE_TASK_MISSING` + `provider_task_not_found`, no task-id/home/message/cause reflection) were verified intact. New tests: `packages/server/test/provider-index-composition.test.ts` (3) proves flag-off never registers a home, the false->true transition registers each trusted home exactly once with a path-free result, and repeat/flip-back transitions never rebuild; `coordinator-rebuild.test.ts` gains the full-DB clear/rebuild equivalence case (clearing the entire DevHub DB leaves fake native snapshots unchanged; a fresh DB rebuilds byte-equivalent indexed tasks). Changed files: `packages/server/src/app.ts` + the two test files only. Focused gates: engine provider-index 629/629, full server 207/207 (incl. the 3 new), web provider-api 52/52; engine/server/web typecheck + `git diff --check` clean; four user-owned paths untouched. Independent SPEC + QUALITY/SECURITY GO recorded below. Flags unchanged (`nativeCodex`/`persistentClaude`/`unifiedTaskIndex` false).
- M5-T5-INDEXED-ROUTES [DONE 2026-07-15] Authenticated, locator-only indexed route table landed on `wip/devhub-background-runner` (NOT promoted to `campaign/auto-improve`). New `packages/server/src/routes/provider-index.ts` registers the full table: `GET homes`, `GET tasks` (opaque `pi1.` cursor + full sort tuple + scope carried by the engine store; list order updatedAt DESC NULLS LAST, provider ASC, homeFingerprint ASC, nativeTaskId ASC; repeated pages neither skip nor duplicate; scope-change-with-cursor rejected by the store), `GET tasks/:locator` (freshness=native|cache via `coordinator.readThrough`, missing->404 `NATIVE_TASK_MISSING`, plus `store.getReconciliation`), start/resume/fork/send/steer/interrupt/respond/archive/rename (resolve locator->canonical home via `store.resolveHome`, dispatch through the registry, project path-free via readThrough, invalidate cache on uncertainty), `GET events` SSE (subscribe-first-into-bounded-buffer -> authoritative snapshot from readThrough with fresh random stream epoch -> drain buffered live events dropping request-id/(turn,item,type) keys already in the snapshot -> atomic switch to live; buffer/queue overflow or failure emits a value-free `resync-required` terminal and closes; every Last-Event-ID is ignored so each connection takes the full path with no prior-connection replay), `GET reconciliation` + `POST reconciliation/ack` (authoritative reread -> nullable observed native fingerprint -> exact store CAS), `PATCH meta` (additive-only, store-enforced), and `POST rebuild` (single-flight; `REBUILD_BUSY`->409). All mutation/control/rebuild/meta/ack routes require a configured bearer token + trusted origin and reject query-string tokens; read/list/SSE honor the existing configured-token/origin policy. Homes are registered only by trusted startup: runtime discovery, the new `BuildOptions.providerHomes` explicit source, or `DEVHUB_*_HOME`/legacy env via the runtimes — no browser endpoint accepts a raw home. Server composition (`app.ts`) derives a backend-only `indexedRegisteredHomes` fingerprint view (canonical home kept only to match the registry census / resolve locators, never returned), registers the routes only when the shared store exists, and reports `availableDevHubFeatures.unifiedTaskIndex` true only when that store exists; `syncProviderTaskIndex` is now fail-open (a failed coordinator init leaves it null and every indexed route reports the feature disabled). Legacy `provider-tasks.ts` stays byte-compatible when the flag is off and now best-effort observes successful list/read/start/resume/fork results into the cache when a coordinator exists (no-op + unchanged bytes when it does not). Web adds `apps/web/src/lib/provider-index-api.ts` (locator-only facade over `/api/provider-index/*`, path-free, with `selectProviderTransport`/`isUnifiedTaskIndexApplied` choosing the facade ONLY when the flag is applied true, else the existing direct key-based `providerApi`); the flag is never defaulted on. New tests: `packages/server/test/provider-index.test.ts` (25, incl. flag gating, all auth modes, homes, list+cursor+scope-change+ordering/pagination, read+freshness+missing, start/send/archive/rename/steer/interrupt/respond, reconciliation get+ack CAS, additive meta, single-flight rebuild, SSE snapshot->live + Last-Event-ID full path, and a recursive no-raw-home/no-NUL scan across every success/error/SSE surface); `apps/web/src/lib/provider-index-api.test.ts` (7, facade URLs/methods/bodies path-free + transport selection). Focused gates: server 232/232 (was 207; +25), web 180/180 (was 173; +7); engine/server/web typecheck + `git diff --check` clean; no NUL bytes in any authored file. Independent SPEC + QUALITY/SECURITY GO recorded below (M5-T5-REVIEW 2026-07-15: re-verified server provider-index 25/25, full server 236/236, web facade 7/7, both typechecks PASS, recursive no-raw-home/no-NUL scan clean; VERDICT GO, no gap). Flags unchanged (`nativeCodex`/`persistentClaude`/`unifiedTaskIndex` requested-default false).
- M5-T6-ARCHIVE-V2 [DONE 2026-07-15] Portable archive v2 authority separation + v1 compatibility landed on `wip/devhub-background-runner` (NOT promoted). `packages/engine/src/portable.ts` rewritten: the DEFAULT `exportArchive` now emits `DevHubArchiveBundleV2 {kind:'devhub-archive',schemaVersion:2}` carrying ONLY authority-clean data — additive legacy sidecar meta (`legacyMeta`, session_meta keyed by sessionId, NO `sessions` rows/transcript paths), `savedViews`, `audit`, `providerTaskMeta` (locator-addressed local metadata), `providerForkLinks` — and NEVER a provider task cache row, mirrored message text, raw home path, transcript path, credential, or hidden reasoning (proven by a serialize+scan test). Legacy `LegacyArchiveBundleV1 {kind:'claude-ui-archive',schemaVersion:1}` still imports; a NEW `exportLegacyV1Archive` emits the v1 bundle for ROLLBACK ONLY, restricted to the UNRESOLVED legacy corpus (any sessionId in `legacy_session_task_map` is excluded; explicitly selecting a resolved one throws `ArchiveValidationError`), never default, never emits the unified cache; the transport labels it `X-DevHub-Archive-Authority: legacy-rebuildable-cache`. Import dispatches on discriminator+version: v2 restores ONLY additive metadata + locator links (never a cache row — test asserts `provider_task_cache` count 0); provider metadata lands as ORPHANED locators unless the caller supplies a validated `ArchiveHomeMapping` (each source `(provider,fingerprint)` → at most one SAME-provider REGISTERED target; duplicate-identical collapses; unknown-target / provider-change / conflict / cycle / malformed rejected BEFORE any write; many-to-one allowed only when collapsed per-locator content is identical; differing content on one target = counted `MAPPING_COLLISION` aborting the whole transaction pre-BEGIN, no last-writer-wins). v1 import preserves the historical restore but records every restored session as `archive-v1-import` provenance and NEVER claims native ownership or overwrites a verified live mapping (skips provenance for a session that already has a `legacy_session_task_map` row). `ImportArchiveResult` returns exact counts (`sessions/meta/textRows/savedViews/audit/providerMeta/forkLinks/mappedLocators/orphanedLocators/legacyProvenance`). Both formats bounded (array + UTF-8 string caps), schema-validated, idempotent (PK upserts), transactional (single BEGIN/COMMIT with ROLLBACK-on-throw), secret-clean. Server `routes/portable.ts`: shape gate accepts either envelope; `GET /api/export/archive` defaults to v2 (`devhub-archive-v2.json`), `?format=legacy-v1` emits the labeled rollback bundle; `ArchiveValidationError`→400. Engine barrel + `TranscriptIndex`/`Engine` expose `exportLegacyV1Archive` + the new types/errors. New TDD in `packages/engine/test/portable.test.ts` (19: default-v2 no-content/no-path, v2 round trip + exact counts, idempotency, chunk reassembly, cross-machine mapping, unknown-target/provider-change/conflict/duplicate/cycle, many-to-one collapse vs differing-content abort/rollback, mixed-provider isolation, golden v1 reader compat + provenance, v1 no-live-overwrite, legacy-only export + resolved-selection rejection, discriminator/version mismatch, bomb-like bounds, raw-DB module surface, selective v2 scoping) and `packages/server/test/app.test.ts` (portable block reworked: v2 default + no-path, legacy-v1 export + authority header, v2/v1 import round-trips, malformed/incompatible 400s). Changed files: `portable.ts`, `index-db.ts`, `index.ts`, `routes/portable.ts` + the two test files only. Focused gates: engine portable 19/19, full engine 2145/2145, full server 233/233 (app block 77/77); engine typecheck + provider-index public-surface + server + web typecheck clean; `git diff --check` clean; no NUL bytes / no raw-home leak in authored source (v1-only `filePath` is the intended rollback format). Independent SPEC + QUALITY/SECURITY GO recorded below. Flags unchanged (`nativeCodex`/`persistentClaude`/`unifiedTaskIndex` false).
- M5-T7-WEB-COMPAT [DONE 2026-07-15] URL + browser-storage `devhub:*` compatibility migration landed on `wip/devhub-background-runner` (NOT promoted). New `apps/web/src/lib/compat-storage.ts` is the single localStorage seam: `readCompat` tries the DevHub key first then the exact legacy `claude-ui:*`/`claude-ui-token` key and COPIES a legacy hit forward to the DevHub key WITHOUT deleting/rewriting the legacy value (rollback-safe); `writeCompat` targets ONLY the DevHub key (legacy shadowed on next read, never written); `removeCompat` clears BOTH keys so an explicit logout/empty-draft can't be resurrected by the read fallback; every access is SSR-guarded + try/catch (private-mode/quota/disabled-storage/`window.localStorage`-getter-throw all non-fatal); `legacyKeyFor` maps `devhub:` → `claude-ui:` and `devhub-token` → `claude-ui-token`, else null (no guessed mapping). Migrated EVERY web owner of a `claude-ui:*`/`claude-ui-token` key to `devhub:*` through this seam: `App.tsx` (`devhub:ui`), `FirstRun.tsx` (`devhub:onboarded`), `TranscriptBookmarks.tsx` (`devhub:bookmarks`), `SnippetLibrary.tsx` (`devhub:snippets`), `SettingsPane.tsx` (`devhub:conn`), `SearchPalette.tsx` (`devhub:search-scope`), `hooks/useTheme.ts` (`devhub:theme`), `hooks/useReducedMotion.ts` (`devhub:perf-mode`), `hooks/useRecentSessions.ts` (`devhub:recent-sessions`, storage-event listener now matches either namespace), `hooks/useDraft.ts` (`devhub:draft:` prefix), `hooks/usePromptHistory.ts` (`devhub:prompt-history:` prefix), and `lib/api.ts` (token key now `devhub-token` via the seam; `getToken`/`setToken` route through read/write/removeCompat so a legacy token migrates once and logout clears both). `router.ts` gains an optional `providerTask=<serialized locator>` field appended in stable order (tab, project, session, providerTask); parsing/building validate the locator with a dependency-free grammar mirror (`^pt1\.[a-z][a-z0-9-]*\.[0-9a-f]{64}\.[A-Za-z0-9_-]+$`, ≤1024 chars) and DROP a malformed/NUL-bearing value without throwing — legacy URLs (no `providerTask`) parse unchanged. IMPORTANT: router.ts is eagerly imported by App, so it must NOT pull the engine's Node-only locator module (`serializeTaskLocator`/`parseTaskLocator` transitively import `node:fs` via `task-key.ts`) into the browser bundle; the local regex validator keeps the eager bundle clean while `router.test.ts` feeds a REAL engine-serialized locator (node test env) through the validator to guard grammar drift. `CodexNativePane.tsx`: provider-owned browser state is now path-free + NUL-free — new exported `pathFreeHomeToken` (FNV-1a 32-bit → 8 hex) folds the raw provider home so no filesystem path lands in a draft localStorage key (both the per-task and create drafts now scope by the token, not the raw home) or an in-memory identity; `nativeTaskIdentity` and the request-dedup `identityKey` were rebuilt to hash the home + percent-encode/`|`-join the parts (previously `providerhomenativeTaskId`), removing every raw-home + NUL from those keys. User-owned `ChatPane.tsx` and `SlashPalette.tsx` (and `.gitignore`/`AGENTS.md`) were NOT touched. Focused gates: full web suite 211/211 (was 180; +22 `compat-storage.test.ts`, +7 `router.test.ts`, +2 `CodexNativePane.test.ts` for the path-free identity/token, covering DevHub-first read, legacy copy-forward without delete, write-only-DevHub, remove-both, SSR, quota/private-mode/getter-throw, unmapped key, provider-collision, token-auth end-to-end, providerTask parse/build/round-trip/stable-order/malformed-drop/NUL-drop/legacy-URL-unchanged); web typecheck clean; `vite build` succeeds (proved the eager bundle stays Node-free); `git diff --check` clean; zero NUL bytes and zero raw-home leaks in authored source. SCOPE NOTE (staged, honest): the flag-ON `PublicProviderHome`-only provider setup (never rendering/asking for a filesystem home) is NOT wired here because the `unifiedTaskIndex` locator transport (`selectProviderTransport`/provider-index-api facade) is not yet consumed by any UI component — building it now would be untestable dead code; it is deferred to the Task 9 cutover integration where the transport gets wired. The flag-OFF legacy setup/direct-route path is preserved unchanged as the rollback surface, exactly as the contract requires. Independent SPEC + QUALITY/SECURITY review COMPLETE (M5-T7-REVIEW 2026-07-15: VERDICT GO, contract fully met, no P1/P2, two non-blocking P3 nits, no tracked fix task; gate record below). Flags unchanged (`nativeCodex`/`persistentClaude`/`unifiedTaskIndex` requested-default false; not defaulted on).
- M5-WIRE-LOCATOR-TRANSPORT [DONE 2026-07-15] Wired the `unifiedTaskIndex` locator transport + PublicProviderHome-only setup into the web UI on `wip/devhub-background-runner` (NOT promoted) — the deferred M5-T7 scope-note item. `CodexNativePane` is now a thin flag gate: `isUnifiedTaskIndexApplied(features)` true → NEW `apps/web/src/components/ProviderHomeSetup.tsx` (calls `selectProviderTransport(features)`; discovers homes ONLY via the path-free `provider-index-api` facade `homes()` → `PublicProviderHome {provider,homeFingerprint,status,capabilities}`, never a raw filesystem home, so it can neither render nor request one; renders a fingerprint-only picker + capability summary), false/undefined → the unchanged monolith renamed `CodexNativeDirectPane` on the direct key-based `providerApi` (byte-for-byte rollback surface). Component SWAP (no shared hooks across a flip). `App.tsx` passes `features={settings?.devHubFeatures}` to both mounts. BUILD FIX: consuming the facade dragged its only runtime engine import (`serializeTaskLocator`, which pulls `node:fs`/`realpathSync` via `task-key.ts`) into the browser graph and broke `vite build`; replaced with a browser-safe local `serializeTaskLocator` (`TextEncoder`+`btoa`, exact `pt1.<provider>.<64-hex>.<base64url(id)>` grammar) mirroring the M5-T7 router regex pattern, guarded by a byte-identical-vs-engine drift test. Tests: NEW `ProviderHomeSetup.test.ts` (12: transport selection by flag state, `discoverProviderHomes` path-free facade wiring, real-facade `homes()` GET/no-body/no-raw-home, flag-ON DOM fingerprint-only with zero raw-home/NUL, single-home no-select, pure helpers, and flag-off wrapper markup === `CodexNativeDirectPane` byte-for-byte) + `provider-index-api.test.ts` 7→8 (grammar drift). FULL web 224/224 (14 files, was 211), web `tsc --noEmit` PASS, `vite build` PASS (eager bundle stays Node-free with the facade now consumed); `git diff --check` clean; no NUL/control bytes in authored files; four user-owned preservation paths untouched. SCOPE (honest): flag-ON is the SETUP surface only — the full indexed task LIST/READ/SEND/etc. over locators is a larger migration NOT in this unit; and since `nativeCodex`/`persistentClaude` stay FALSE the combined flag-ON pane never mounts live, so nothing regresses in the shipping default. Flags unchanged (`nativeCodex`/`persistentClaude` stay false; `unifiedTaskIndex` requested-default true from the prior cutover).
- M5-T4-9 [RONAK-GATE] After Task 3 promotion, complete Tasks 4-9 serially under their existing review, induced-failure, cutover, and rollback gates.
- M5-T4-WRITER-LEASE [SOFTWARE — PARTIAL, foundation landed 2026-07-15] The narrow fail-closed durable-reconciliation SEAM is done on `wip/devhub-background-runner`: new `packages/engine/src/providers/reconciliation-store.ts` defines `AdapterReconciliationStore` (a provider-facing narrow interface over `provider_reconciliation_state`, keyed by `NativeTaskKey`) plus `createAdapterReconciliationStore(store)`, wrapping the existing `ProviderReconciliationStore`. It carries only the minimal restart-safe latch fields (required / latchRevision / reviewed+native fingerprint / writerEpoch / reason) — no prompts/events/approvals/secrets — and FAILS CLOSED: any delegate read/write throw latches the wrapper permanently `unavailable` (value-free `UNAVAILABLE`, no path/id/secret in the message), never auto-recovers, and an invalid key/latch input surfaces `INVALID_KEY`/`STORE_REJECTED` without touching the delegate or latching unavailable. Exported from the path-free providers barrel. New `packages/engine/test/providers/reconciliation-store.test.ts` (15 adversarial tests: exact locator delegation, minimal-field pass-through, invalid key/input, per-op fail-closed + permanent latch, value-free hostile-throw redaction, Codex/Claude same-id + two-home isolation, dependency-shape rejection, null-fingerprint ack). Engine 2138/2138, typecheck + public-surface + `git diff --check` clean; only the two new files + the barrel `export * from "./reconciliation-store.js"` line changed. STILL REMAINING (not started, pure software, NOT a hard gate): inject `NativeTaskWriterLeaseStore` into `CodexNativeAdapter`, inject the reconciliation store into both production adapters, rewire all existing-task mutations (resume/fork/send/steer/interrupt/respond/archive/rename) through acquire-lease -> reread native revision -> compare-to-reviewed -> confirmReread -> runFencedWrite with latch-on-failure/no-replay-after-uncertainty, durable acknowledge CAS ordering, and the remaining ~15 adversarial scenarios (two-process race, expired takeover, ABA epoch, crash/timeout before+after send/respond, heartbeat loss, callback reentrancy, hostile clocks, generation restart, both-providers-restart durable latch, relatch/CAS-refusal). Not promoted to `campaign/auto-improve`; `nativeCodex`/`persistentClaude` stay false.
- M5-T4-WIRE [DONE 2026-07-15] Writer-lease + durable reconciliation WIRED into both native adapters on `wip/devhub-background-runner` (NOT promoted). `CodexNativeAdapter` now accepts optional injected `writerLeases` (the exact `NativeTaskWriterLeaseStore` acquire seam Claude uses -- no second competing lease) + `AdapterReconciliationStore`; all 8 existing-task mutations (resume/fork/send/steer/interrupt/respond/archive/rename) run through the fenced path: guard durable+in-memory latch -> acquire/validate the exact task writer lease (monotonic epoch; ABA/stale/lost rejected) -> reread exact native revision -> compare to last reviewed -> confirmReread -> runFencedWrite -> post-dispatch generation check -> update reviewed. Any mismatch / lost lease / deletion / missing-persisted / generation change / thrown parser / uncertain post-dispatch outcome fails closed with RECONCILIATION_REQUIRED, latches durably (never replays), and a durable read/write fault fails closed + marks the unified runtime unavailable (isAvailable consults store.unavailable). `respond` routes through the fence when its identity resolves (the broker already rejects stale request/generation before dispatch). Added `acknowledgeReconciliation` (authoritative reread -> store CAS -> in-memory clear). `ClaudeNativeAdapter` gains an optional `reconciliationStore` (purely additive/gated): mirrors every in-memory latch durably, restores a durable-required latch before any post-restart mutation (in `writerForMutation` + `acknowledgeReconciliation`), durable-CAS clears before the in-memory clear on ack, and fails closed on a store fault. Both runtimes construct/inject: `native-codex-runtime.ts` opens/ shares the `native-task-writer-leases.sqlite` store (owned-close on shutdown; injectable to share Claude's exact instance) and wraps an injected raw reconciliation store via `createAdapterReconciliationStore`; `native-claude-runtime.ts` wraps + injects the reconciliation store. Focused gates: Codex adapter 44/44 (+9 fenced: 5-mutation fence routing, resume fence, revision drift, deletion, lost-lease no-replay, confirm-fail, fenced-write-not-started, uncertain no-replay, durable-fault -> unavailable), Claude adapter 126/126 (123 existing ZERO regressions + 3 durable-mirror: mirror / restart-restore / store-fault), full engine 2157/2157, full server 233/233; engine + server typecheck + `git diff --check` clean; only the 2 adapters + 2 runtimes + 2 adapter test files touched; four user-owned paths untouched; `nativeCodex`/`persistentClaude` stay false. STILL REMAINING for full Task 4 (deferred, pure software, NOT a hard gate): the app.ts injection of the shared stores against the REAL `ProviderTaskIndexStore` + independent adversarial review is Task 5/9 cutover wiring. The adversarial test matrix itself is now COMPLETE — see M5-T4-ADVERSARIAL below.
- M5-T4-ADVERSARIAL [DONE 2026-07-15] Remaining Task 4 adversarial writer-lease/reconciliation scenarios added on `wip/devhub-background-runner` (NOT promoted; test-only, no source touched). Extended `packages/engine/test/providers/codex-native-adapter.test.ts` (44 -> 62 tests, +18) with a new `adversarial writer-lease + reconciliation parity` suite plus reused fakes (added `FakeWriterLease.forceLost`, `FakeWriterLeaseStore.epochSequence`/`markLostOnReacquire`+per-key live-handle tracking, `FakeReconciliationStore.seedRequired`/`relatchOnAck`, `fencedAdapter` maxTrackedRevisions passthrough). New scenarios: two adapters/processes racing one key (takeover fences the stale holder mid-reread; monotonic epochs 1,2; loser never writes); Codex/Claude same-nativeTaskId isolation (anthropic latch never blocks openai + reverse); non-monotonic/ABA writer epoch rejected before any reread or dispatch; hostile-clock tampered fence epoch -> OWNERSHIP with no dispatch; heartbeat-loss (fenced write cannot start) -> WRITER_LEASE_LOST + lease released + no dispatch; crash/timeout BEFORE native send (lost lease) never dispatches and never replays; process-generation restart mid-dispatch -> PROCESS_GENERATION_CHANGED latch, single turn/start, no replay; live-callback reentrancy during a fenced dispatch stays consistent (event delivered, mutation succeeds, no spurious latch); respond routes through the fence and correlates the exact approval once; stale approval respond is a fenced no-op that never latches or replays; durable-latch restore on a fresh (restarted) adapter blocks before any mutation; a latched task survives in-memory cache eviction pressure; same-fingerprint relatch increments the durable latch revision across clear->relatch; newer durable latch refuses a stale-revision acknowledge (CAS refusal, stays latched); acknowledge with a mismatched reviewed fingerprint stays latched; exact-fingerprint acknowledge clears durable+live latch and re-enables mutations; durable-store faults on requireReconciliation and on acknowledgeReconciliation both mark the runtime unavailable (DISABLED). Focused gate (DoD): Codex + Claude lease suites run TOGETHER green at 2 forks -> 250/250 (`codex-native-adapter` 62, `claude-native-adapter` 126, `writer-lease` 22, `reconciliation-store` 15, `codex-native-revision` 5, `claude-native-revision` 20) proving zero cross-provider regression; engine typecheck + provider-index public-surface + `git diff --check` clean; only the one codex adapter test file changed. `nativeCodex`/`persistentClaude`/`unifiedTaskIndex` stay false.
- M5-T9-DRILLS [DONE 2026-07-15] Task 9 gate-2 induced-failure drill suite landed on `wip/devhub-background-runner` (NOT promoted). New consolidated `packages/engine/test/provider-index/gate2-drills.test.ts` (10 tests) directly reproduces the cross-cutting gate-2 properties against real engine APIs (v13 migration recovery + idempotent rerun after reset-to-13; flag rollback = explicit-stored-false wins + migrations never down-migrate/drop provider tables; DB busy/write failure = store mutation inside a caller-owned `BEGIN IMMEDIATE` fails closed `DATABASE_UNAVAILABLE` with zero partial write, then recovers; invalid/unknown-fingerprint locator rejection via `parseTaskLocator` + `resolveHome` null/`INVALID_INPUT`; cursor scope abuse via `serialize`/`parseProviderIndexCursor` cross-scope + tampered-checksum rejection; path-free 64-hex home fingerprint). The full gate-2 list is mapped item->passing test(s) in `evidence/m5/gate2-drills.manifest.json`; the remaining items are covered by the existing RED-first adversarial suites (migration, store, store-active-cache, store-stage/-write, store-codec, store-observation, cursor, coordinator-rebuild/-readthrough, identity, portable, codex/claude native adapters, reconciliation-store, writer-lease, feature-flags, server provider-index + app). New runner `evidence/m5/run-gate2-drills.sh` runs the corpus at `--maxWorkers=2` and stages `{engine,server}-drills.{json,log}` + `gate2-drills.SUMMARY.md`: engine 806/806 (18 files) + server 105/105 (2 files) = 911/911 green, NO provider process spawned. engine typecheck + public-surface tsc PASS; `git diff --check` clean; only the one new test file + `evidence/` added; four user-owned paths untouched. Flags unchanged (`nativeCodex`/`persistentClaude`/`unifiedTaskIndex` false). The gate-2 drills are one of Task 9's five pre-cutover gates; the feature-flag default flip (cutover) remains a hard RONAK-GATE and was NOT performed.
- M5-T9-STAGE-CUTOVER [DONE 2026-07-15] Final independent M5 reviews + STAGED `unifiedTaskIndex` cutover prepared; default-on flip held as the single [RONAK-GATE] human action. Work committed on `wip/devhub-background-runner` (STATUS + `evidence/m5/` only — the flag stays FALSE on wip). FRESH SPEC review GO (no missing M5 requirement: invariants 1-12 + Tasks 1-9 each map to a landed tested surface). FRESH ADVERSARIAL review GO (no open P1/P2; two prior T7 P3 nits stay accepted; one new cutover-honesty P3 FIXED in the cutover commit by tying `appliedDevHubFeatures.unifiedTaskIndex` to `coordinator !== null`; no unaccepted P3). Gate-5 checks all pass (`evidence/m5/precutover-checks.md`): `git diff --check` clean, targeted secret scan clean (only synthetic redaction fixtures in tests), raw-home/NUL scan of evidence artifacts clean, generated-file check clean, preservation hash/status clean (four user-owned paths untouched, matrix unchanged). STAGED cutover on ISOLATED branch `staging/m5-t9-cutover` commit `01f39e4` (base reviewed tip `f4d27fd`; NOT `campaign/auto-improve`, NOT `origin/main`, NOT `wip/devhub-background-runner`): flips `DEFAULT_DEVHUB_FEATURE_FLAGS.unifiedTaskIndex` false->true (6 files). Requested default ON; resolved value clamped to `requested && available(store exists) && applied(coordinator initialized)`; explicit stored `unifiedTaskIndex:false` wins over the default (non-destructive rollback, no down-migration, legacy routes byte-compatible). Focused flag/settings/routes/rollback tests GREEN on `01f39e4` at 2 workers: engine 4/4, server settings-features 8/8 + provider-index 25/25 + provider-index-composition 3/3 = 36/36 (40 focused), app.test.ts 80/80 unaffected; engine+server+public-surface tsc PASS; `git diff --check` clean. Explicit stored-false rollback rehearsed in `provider-index-composition.test.ts`. Evidence: `evidence/m5/cutover-staged.SUMMARY.md`, `precutover-checks.md`, `cutover-{engine-flags,server-routes}.{json,log}`. REMAINING single human action: promote `01f39e4` + run the FULL exact-tip heavy-queue engine/server/web gate with forced cache bypass + capture Browser/Computer-Use QA evidence under `evidence/m5/`. `nativeCodex`/`persistentClaude` stay false (separate billable live-proof gates).
- DEVHUB-FLIPS [DONE 2026-07-15, RONAK-DIRECTED] Flipped `DEFAULT_DEVHUB_FEATURE_FLAGS.nativeCodex` false->true on `wip/devhub-background-runner` (M3 native-Codex cutover; live resume proof already captured in `evidence/m3/live-runtime-resume-proof.md`), and completed the M5 `unifiedTaskIndex` strangler cutover default-on (already true on wip from M5-FINALIZE-CUTOVER — reconfirmed under the full heavy-queue gate WITH the nativeCodex flip applied). Both defaults are REQUESTED-only: the server still clamps each resolved value to real runtime availability (`availableDevHubFeatures.nativeCodex = nRuntime !== null`; `unifiedTaskIndex` applied only when the shared store exists AND the coordinator initialized), and an explicit stored `false` is the immediate non-destructive rollback (history mode / legacy byte-compatible provider routes). Source change is 1 line in `packages/engine/src/providers/feature-flags.ts` (+ its doc comment); test fallout was purely default-proxy assertions, fixed preserving intent: `feature-flags.test.ts` default-object assertion now expects `nativeCodex:true` and the override test switched to `persistentClaude:true` (a still-false flag), and `native-codex-runtime.test.ts` rollback step now sets `nativeCodex:false` explicitly instead of relying on the bare default (mirrors the M5-FINALIZE-CUTOVER gate2-drills fix). FULL gates green: engine 2204/2204, provider-index subset 639/639, server 236/236, web 478/478; engine tsc + provider-index public-surface + server tsc + web tsc all PASS; `git diff --check` clean. `persistentClaude` stays false (its own M4 billable live-proof gate). HELD (unchanged): the `origin/main` merge and the `campaign/auto-improve` promotion remain [RONAK-GATE] human actions — this landed on `wip/devhub-background-runner` only; the live M1 Gateway cutover stays held.
- M6-IMPLEMENTATION-PLAN [DONE 2026-07-15] Authored `.planning/devhub-codex-parity/m6-implementation-plan.md` on `wip/devhub-background-runner` (NOT promoted; DOC-ONLY, no code/flags changed). Mirrors the m5 plan structure and decomposes `design-lock.md` §2 into the locked strangler slice order (DevHubShell+chrome → TaskRail → TaskHeader+provider-aware setup → ThreadWorkspace+ActivityTimeline → stable Composer → InspectorDock → Search/command palette → Settings/secondary, then a Task 9 App.tsx composition-reduction + `codexStyleShell` cutover). Each slice specifies a RED-first DoD, its governing reference capture + concept, the exact `component-state-matrix.md`/`surface-inventory.md` owners, its own default-OFF slice flag, the required `fidelity-ledger.md` entry, and per-slice gate criteria (current/min/narrow screenshots, >=5 comparison points, visible-copy diff, keyboard/a11y). Frozen invariants recorded (provider immutable after creation; capability-gated controls never CSS-faked; preserved surfaces never disappear; no dashboard/marketing chrome in the task canvas; + measured-geometry/shadcn/brand/a11y rules). Validation: no NUL/control bytes; all referenced planning files, 5 captures, and 8 concepts resolve; `git diff --check` clean; four user-owned paths untouched. The M6 milestone itself stays [RONAK-GATE] — building any slice requires explicit resume. `nativeCodex`/`persistentClaude` stay false.
- M6-T1-SHELL-GEOMETRY [DONE 2026-07-15] DevHubShell + application chrome built behind the NEW default-OFF `shellChrome` slice flag on `wip/devhub-background-runner` (NOT promoted). New `features/shell/DevHubShell.tsx` renders the locked design-lock §4 geometry (273 rail / 46 header / open `#181818` canvas / shared 736 transcript+composer column / 300 content-height inspector dock at 12-top/16-right/16-radius / 16 outer gutter) from `design-system.md` `--dh-*` tokens only, with shell landmarks (one main, named rail nav, optional named complementary inspector) + skip link + exact `DevHub` wordmark, `RegionBoundary` provider-failure isolation, status-invariant geometry, and single-rail responsive collapse (no hidden duplicate tabbable). New `features/shell/AppShell.tsx` is the composition seam; `App.tsx` routes to it via `resolveShellChromeMode(settings)` with the legacy chrome byte-for-byte under flag-off and DevHubShell instantiated ONLY under flag-on. `shellChrome` flag added default-false to engine flags + server settings route (keys/schema/complete/applied) + web complete. Geometry unit/snapshot + flag-off-preservation tests: web `DevHubShell.test.ts` (14) + `AppShell.test.ts` (4). FULL: web 242/242, server 236/236, engine 2204/2204; 3 typechecks + `vite build` PASS; `git diff --check` clean; no NUL bytes; four user-owned paths untouched. Browser QA (Playwright, measured geometry) at 1800x1130 + 768 + 390 with 11 comparison points all Match; evidence under `evidence/m6/shell/`; `fidelity-ledger.md` M6 §/Task-1 table added. SCOPE: frame geometry/tokens/landmarks/flag-safety only — rail/header/transcript/composer/inspector CONTENT are placeholder slots owned by M6 Tasks 2–8. `nativeCodex`/`persistentClaude`/`shellChrome` requested-default stay false.
- DEVHUB-SHADCN [DONE 2026-07-15, RONAK-DIRECTED] Installed shadcn/ui + Radix as the app's sanctioned component library on `wip/devhub-background-runner` (NOT promoted). Added `apps/web` deps (`class-variance-authority`, `@radix-ui/react-slot|react-tooltip|react-dropdown-menu|react-dialog|react-scroll-area|react-separator`; `clsx`/`tailwind-merge` already present); wired the canonical `@/`→`src` alias into `tsconfig.json` + `vite.config.ts` + `vitest.config.ts` (identical resolution across build/typecheck/tests); added `components.json` (new-york, Tailwind v4, cssVariables, lucide); generated `src/components/ui/{button,textarea,badge,separator,tooltip,dropdown-menu,dialog,scroll-area}.tsx` on the existing `cn` (`src/lib/utils.ts`); and added ADDITIVE shadcn semantic tokens to `index.css` via `@theme inline` mapped to the existing dark palette (`bg-primary`/`border-border`/`ring-ring`/… = the current clay/zinc) WITHOUT redefining the legacy `--accent`/`--border` custom properties, so nothing already on screen changes. Slice adoption: `TaskRail` composes its row className through shadcn's `cn`. New tests: `src/components/ui/ui-primitives.test.ts` (9 — cn merge/dedupe, Button variants + `asChild` Radix Slot + native-prop passthrough, Badge, Textarea). FULL web 487/487 (was 478; +9), web `tsc --noEmit` PASS, `vite build` PASS; `git diff --check` clean. SCOPE NOTE (honest, staff-approved boundary): the pixel-locked M6 shell slices (DevHubShell/TaskRail/TaskHeader/Composer) were NOT reimplemented on STYLED Radix primitives. Their design-lock tests pin exact `dh-*` classes, forbid inline geometry styles, and treat utility-class substrings (e.g. `disabled:`) as failures; the node-env `renderToStaticMarkup` harness cannot render Radix portal content (dropdown/tooltip/dialog). shadcn's default Button/Textarea inject exactly the forbidden visual utilities (`bg-primary`/`h-9 px-4`/`rounded-md`/`disabled:opacity-50`), which would silently break the measured composer/rail visuals while DOM-only tests stayed green — a design-lock regression. shadcn is therefore the sanctioned library for non-locked/future UI; a deeper Radix DOM migration of the locked slices is a separate decision that requires relaxing the M6 pixel-lock DOM tests (Ronak's call). `nativeCodex`/`persistentClaude`/`shellChrome` unchanged; all hard-gate holds intact (no `origin/main` merge, no `campaign/auto-improve` promotion, live M1 cutover held).
- M6 [RESOLVED 2026-07-16] Resume the approved reference-first shell only after Ronak resumes the goal and M5 is complete. STATUS UPDATE 2026-07-15: Ronak's software-implementation authorization superseded the pause for build work (not for the umbrella cutover). All nine Codex-style-shell slices are now BUILT + TESTED behind their own default-OFF flags on `wip/devhub-background-runner`, commits `4b97893..ad2f3e5`: Task 1 `shellChrome` (DevHubShell + app chrome), Task 2 `taskRail` (open-list rail), Task 3 `taskHeaderSetup` (TaskHeader + provider-aware setup), Task 4 `threadWorkspace` (ThreadWorkspace + ActivityTimeline), Task 5 `composerSurface` (geometry-stable Composer), Task 6 `inspectorDock` (InspectorDock), Task 7 `searchCommands` (Search + Command dialogs), Task 8 `settingsSecondary` (SettingsRoute + Ops/Inbox/Dashboard secondary routes), Task 9 `M6-T9-COMPOSE` (wired all eight slices' deferred data-wires into `App.tsx` composition). Every slice keeps a non-destructive rollback to its legacy component via an explicit stored `false`. STATUS UPDATE 2026-07-16 (M6-CODEXSTYLESHELL-CUTOVER): the umbrella `codexStyleShell` + all 8 slice flags are now flipped requested-default-true on `wip/devhub-background-runner` (see the top-of-file entry + `evidence/m6/cutover/`). No longer blocked. QUALITY NOTE carried forward from M6-SLICE-EVIDENCE (still real, not fabricated-green, does not block this cutover but DOES block any FURTHER slice-specific promotion claim beyond "the flag now defaults on"): every M6 3-9 test file renders via `renderToStaticMarkup`/pure-function assertions ONLY — no live `fireEvent`/`userEvent` interaction tests exist yet for keyboard/a11y, and `ThreadWorkspace`/`SettingsRoute` still lack a real narrow-viewport test fixture (only a demo-harness screenshot). `origin/main` merge / `campaign/auto-improve` promotion stay held per scope (not requested by this task).
- M7 [RONAK-GATE] Resume cross-provider fork/Work-mode/synchronization only after Ronak resumes the goal and M6 is complete.
- M8-SOFTWARE [RONAK-GATE] Resume packaging/performance/preservation/cleanup software work only after Ronak resumes the goal and M7 is complete.
- M8  [RONAK-GATE] If signed/notarized distribution is required and Apple credentials are absent, Ronak must provide/authorize them. Conditional as of 2026-07-14; do not fake-pass signing.
- QA  [RONAK-GATE] First-party `com.openai.codex` Computer Use requires a host build whose allowlist permits that bundle. Added 2026-07-14; DevHub's own browser/Computer-Use QA remains allowed.

No [ALEX], [HARDWARE], or [S3] dependency exists for this project.

## Added after freeze (does NOT count toward %)
- None.

## Active worktrees / WIP branches
- `wip/devhub-background-runner` at `this commit` — PAUSED handoff branch (sole resume target unless Ronak names another checkout). Carries additive, un-promoted WIP built in the background runner: Task 3 M5 coordinator observation lanes (`observeListPage`/`observeTask`, bounded per-locator FIFO lanes, `maxObservationOperations` reservations, per-locator observation epochs). 589/589 provider-index tests, full engine 2083/2083, typecheck + public-surface + `git diff --check` clean. NOT promoted to `campaign/auto-improve`; no independent SPEC/QUALITY/SECURITY GO yet; `nativeCodex`/`persistentClaude`/`unifiedTaskIndex` remain false; no rebuild/read-through/server/flag work. The goal-level pause still stands.
- `campaign/auto-improve` — tested shared branch; M5 E/F plus the FULL Task 3 (native-missing, verified-legacy mapping, opaque observation, atomic-missing, coordinator foundation, observation lanes, T3B readThrough/projector, T3C rebuild, and T3D server-composition wiring) are integrated after exact mainline gates and independent SPEC/QUALITY/SECURITY GO. `nativeCodex`/`persistentClaude`/`unifiedTaskIndex` remain false.
- detached `m5` at `3808d21` — exact reviewed M5 E source retained read-only after promotion; current goal coordinator.
- detached `m5-f` at `4244c39` — historical reviewed F internal-foundation source; read-only and never replayed wholesale.
- detached `m5-facade` at `6e1804c` — exact reviewed M5 F facade source retained read-only after promotion; current goal coordinator.
- detached `m5-task3-contracts` at `537891d` — exact reviewed Task 3 native-missing source retained read-only after promotion; current goal coordinator.
- detached `m5-task3-store` at `364d736` — exact reviewed verified-legacy mapping source retained read-only after promotion; current goal coordinator.
- detached `m5-task3-observation` at `307c223` — exact reviewed Task 3 observation/atomic-missing source retained read-only after promotion; current goal coordinator.
- detached `m5-task3-coordinator` at `1836785` — exact reviewed Task 3 coordinator foundation/initialization source retained read-only after promotion; no lanes/rebuild/read-through/server/flag scope.
- `m5-task3-lanes` worktree — checked out on `wip/devhub-background-runner`; RED-then-green landed for the M5 coordinator observation-lanes slice (`observeListPage`, `observeTask`, FIFO per-locator lanes, `maxObservationOperations` reservations released on all terminal paths, per-locator observation epochs). New test file `packages/engine/test/provider-index/coordinator-observe.test.ts` (12 tests); only `coordinator.ts` and that test changed. Owned paths clean. THEN: M5-T3B landed on the same worktree/branch — a shared backend-only native->indexed projector (`projectNativeTaskSummaryForIndex`/`projectNativeTaskSnapshotForIndex` in `store-codec.ts`, reusing the codec's registered-home/containment/redaction/identity/fingerprint prepare rules) plus `ProviderTaskIndexCoordinator.readThrough({locator,projection,allowDegradedCache})` and `resolveVerifiedLegacySession(sessionId)`. New test file `packages/engine/test/provider-index/coordinator-readthrough.test.ts` (16 tests). Changed files this slice: `provider-index/coordinator.ts`, `provider-index/store-codec.ts`, `index.ts`, and the new test. 605/605 provider-index, 2099/2099 engine, typecheck+public-surface+diff-check clean. THEN: M5-T3C landed RED-first on the same worktree/branch — `coordinator.rebuild({provider,home,signal?})` (bounded rebuild/dedupe/final reads/staging/promotion + heartbeat/cancel/deadline lifecycle; see the M5-T3C DONE entry above). New test file `coordinator-rebuild.test.ts` (23 tests). Changed files this slice: `provider-index/coordinator.ts` and the new test only. 628/628 provider-index, 2122/2122 engine, typecheck+public-surface+diff-check clean. THEN: M5-T3D (wiring + promotion) landed on the same worktree/branch — server composition lazy coordinator on false->true `unifiedTaskIndex` transition (`packages/server/src/app.ts`), the full-DB clear/rebuild equivalence test in `coordinator-rebuild.test.ts` (now 24 tests), and a new `packages/server/test/provider-index-composition.test.ts` (3 tests). Server 404 DTO and web safe-code/public-tag allowlist verified intact. 629/629 provider-index, server 207/207, web provider-api 52/52; engine/server/web typecheck + diff-check clean. The full Task 3 diff was then promoted onto `campaign/auto-improve`.

## M5-T3B independent gate record (2026-07-15)
- SPEC GO — Matches the T3B contract: home resolution (store `UNKNOWN_HOME`) before provider access; native success wins and writes through under the pre-captured opaque observation token (pre-promotion returns the frozen path-free projection with no published generation); authoritative-missing (`NATIVE_TASK_MISSING`) calls `markNativeTaskMissing(token)` in the store's owned txn and never consults cache/gzip; other failures drop the token without mutation and use cache only when `allowDegradedCache` and `cacheDetail` matches (snapshot requires `cacheDetail:'snapshot'`; summary consumes either) else rethrow the registry-sanitized provider error with cache corruption outranking it; token drift fails `RECONCILIATION_CAS_MISMATCH`; `RECONCILIATION_REQUIRED` never enters the missing transition. Scope note: verified-legacy routing is engine-only here (delegates to `getVerifiedLegacySessionMapping`, which is written only post-native-verification via `mapVerifiedLegacySession`; unresolved provenance returns null → stays on the legacy path). Broader sessionId→adapter wiring stays with later Task-3 server/web units.
- QUALITY GO — Projector reuses the codec `prepare*` rules (no rule duplication); token lifecycle is a single `try/finally` with a tolerant post-commit drop; small explicit field-maps for summary/cache projections; removed now-dead `void this.registry`. Focused RED-first suite; no changes outside authored files.
- SECURITY GO — Outputs are path-free (canonical home used only to build the backend registry key/registration, never returned); errors stay value-free/sanitized (tests assert no `/tmp/`); the opaque token is never exposed by the coordinator and atomic CAS prevents a late provider result from overwriting/deleting newer authority; hostile `readThrough` input is rejected before any provider access.

## M5-T3D (wiring + promotion) independent gate record (2026-07-15)
- SPEC GO — Matches the Task 3 wiring contract: server composition builds a frozen `registeredHomes` `{provider,home}` array only from the trusted native-runtime installs used to construct the adapters (no new raw-home accessor), and creates+initializes the coordinator LAZILY on each false->true effective `unifiedTaskIndex` transition (idempotent single build; also honored at `onReady` when already effectively on). `TranscriptIndex` keeps sole ownership of the shared store and never constructs a coordinator; the concrete coordinator/factory is exported only from the root engine, while the providers barrel stays path-free (no store/configured-home input/raw-home carrier/stage-lease/projector). Server maps `NATIVE_TASK_MISSING` to HTTP 404 with the exact body `{error:'provider_task_not_found',code:'NATIVE_TASK_MISSING',provider}`, and the web safe-code/public-tag allowlist retains that code/body without reflecting task id/home/remote message/cause. Flag-off routing never instantiates or calls the coordinator read path (proved). The full-DB clear/rebuild equivalence property holds: clearing the entire DevHub DB leaves fake native snapshots unchanged and a fresh DB rebuilds byte-equivalent indexed tasks. `unifiedTaskIndex` requested default stays false.
- QUALITY GO — The wiring is a small, single-purpose `syncProviderTaskIndex(unified)` closure plus a frozen `registeredProviderHomes` array reusing the existing runtime `installation.home`; no new accessor, no duplication of the engine factory's reject/sort/sample/register/freeze logic. Tests reuse the existing hermetic buildApp and rebuild-fixture harnesses; only `packages/server/src/app.ts` and two test files changed. Focused gates: engine provider-index 629/629, server 207/207, web provider-api 52/52; engine/server/web typecheck + `git diff --check` clean.
- SECURITY GO — Canonical homes stay backend-only: they enter only `store.registerHome` and are never returned (the composition test asserts the registration JSON never contains the raw home). Flag-off never builds or reaches the coordinator, so rollback trivially restores legacy direct reads. The 404 DTO and web allowlist expose only stable value-free tags. No provider process is spawned by the tests (native runtimes are never enabled). Four user-owned paths untouched.

## M5-T5 (indexed routes) independent gate record (2026-07-15)
- SPEC GO — Every Task 5 requirement verified against `packages/server/src/routes/provider-index.ts` + `apps/web/src/lib/provider-index-api.ts`. FULL ROUTE MATRIX present: `GET homes`, `GET tasks` (list), `GET tasks/:locator`, `POST tasks` (start), and per-locator `resume/fork/send/steer/interrupt/respond/archive/rename`, `GET tasks/:locator/reconciliation`, `POST .../reconciliation/ack`, `PATCH .../meta`, `POST rebuild`, `GET tasks/:locator/events` (SSE). AUTH: `mutationAuthHook` (configured Bearer + trusted origin, 503 when no token configured) guards start/resume/fork/send/steer/interrupt/respond/archive/rename/ack/meta/rebuild; `readAuthHook` guards homes/list/read/reconciliation-GET/SSE; BOTH call `rejectsQueryToken` so a `?token=` query is 401 on every route (no query-string tokens anywhere). LOCATOR-ONLY DTOs: responses carry only `{provider,homeFingerprint,nativeTaskId}` locators / fingerprint homes; canonical home is used solely to key the registry census (line 552) and resolve locators, never serialized. LIST + CURSOR: route forwards scope (`provider`/`homeFingerprint`/`includeArchived`/`limit`/`cursor`) to `store.list`; ordering (updatedAt DESC NULLS LAST, provider ASC, homeFingerprint ASC, nativeTaskId ASC) + no-skip/no-dup proven by the route test's 5-item paginated walk (`["task-4"..."task-0"]`, `Set.size===5`); opaque `pi1.<base64url>.<sha256>` cursor and SCOPE-CHANGE-WITH-CURSOR REJECTION are store/cursor-enforced (`cursor.ts assertPositionInScope` → TypeError → route 400) and engine-tested (`cursor.test.ts` "rejects cursor reuse under any different scope", `store-active-cache.test.ts` "binds cursor scope"); homeFingerprint-without-provider is a route-level 400. READ: `freshness=native|cache` via `coordinator.readThrough`, missing → 404 `NATIVE_TASK_MISSING`, plus `store.getReconciliation`. SSE: subscribe-first-into-bounded-buffer → authoritative snapshot with fresh random `streamEpoch` → drain-with-dedup (drops request-identity and `(turn,item,type)` keys already in the snapshot) → atomic switch to live; buffer/queue overflow or failure emits a value-free `resync-required` terminal and closes; Last-Event-ID is never read, so every connection takes the full path (test uses `stale-foreign-id`). RECONCILIATION: authoritative reread → nullable observed native fingerprint (null on `NATIVE_TASK_MISSING`) → exact `store.acknowledgeReconciliation` CAS (409 on mismatch). META: `hasExactMetaPatchShape` additive-only allowlist, empty/unknown-field → 400. REBUILD: single-flight `coordinator.rebuild`, `REBUILD_BUSY` → 409 (proven by the gated concurrent-rebuild test). FLAG-OFF BYTE-COMPATIBILITY: `provider-tasks.ts` legacy responses are unchanged; cache-warming is a fire-and-forget `void Promise.resolve().then(observe).catch()` no-op when `getCoordinator()` returns null; `unifiedTaskIndex` requested-default stays false and indexed routes are registered only when the shared store exists, returning `unified_task_index_disabled` 404 while the coordinator is null.
- QUALITY GO — Route file is single-purpose and small-diff: shared `exactBody` preValidation + AJV schema pair per route, one `sendIndexError` mapping every store/coordinator/adapter error class to a stable code, best-effort `observeTask`/`invalidate` helpers that never throw into a response. SSE state machine is self-contained with explicit `reserved`/`stopped`/`accepting`/`live`/`waitingForDrain` flags, bounded `preSwitchBuffer`/`queue` byte caps, and a single `releaseReservation` path proven by the concurrent-stream reservation test. Web facade mirrors the surface path-free with an `assertLocatorShape` guard that rejects any accidental `home` field. Tests reuse a hermetic Engine (temp SQLite) + non-spawning explicit Codex install + fake registry; no native runtime is enabled. Real counts THIS review: server provider-index 25/25, full server 236/236, web facade `provider-index-api.test.ts` 7/7; server + web `tsc --noEmit` PASS; `git diff --check` clean. Only the 7 committed files were touched.
- SECURITY GO — Recursive raw-home/NUL scan across every success/error/log/SSE surface is clean. The route test's `assertNoHomeOrNul(response.body, codexHome)` runs on homes, list (each page), native read, missing-404, start, send, reconciliation-required-409, meta, reconciliation-GET, ack-CAS-409, and the SSE snapshot+live frames — none contain the canonical home or a NUL byte. Error responses are value-free stable codes (`RECONCILIATION_REQUIRED`/`NATIVE_TASK_MISSING`/`rebuild_in_progress`/etc.) with no path/id/message reflection. Fastify logger is `false` and the route contains zero `console.`/`req.log` calls, so no log surface can leak a home; locators in the URL path carry only the 64-hex fingerprint. Authored-file NUL grep clean on all 6 source/test files. No provider process spawned. No P1/P2 found; no unaccepted P3.
- VERDICT: GO. No gap, no tracked fix task. M5-T5-INDEXED-ROUTES is clear to promote to `campaign/auto-improve` under its normal promotion gate (flags remain false).

## M5-T6-ARCHIVE-V2 independent gate record (2026-07-15)
- SPEC GO — Matches the archive v2 contract exactly: default `exportArchive` returns `{kind:'devhub-archive',schemaVersion:2}` with additive `legacyMeta` + `savedViews` + `audit` + `providerTaskMeta` + `providerForkLinks` and NO cache row / mirrored text / raw home / transcript path / credential / hidden reasoning (serialize+scan test); `LegacyArchiveBundleV1` still imports; `exportLegacyV1Archive` is rollback-only, excludes any sessionId with a verified unified mapping, rejects explicit selection of a resolved one, is never the default, and never emits the unified cache (labeled `X-DevHub-Archive-Authority: legacy-rebuildable-cache` by the route). v2 import restores only additive metadata/links (never a cache row — asserted 0), orphans provider metadata unless a validated `ArchiveHomeMapping` remaps each source onto a same-provider registered target, with unknown-target/provider-change/conflict/cycle/malformed rejected before any write, duplicate-identical collapse, many-to-one only when collision-free, and differing-content-on-one-target aborting the whole transaction (no last-writer-wins). v1 import records `archive-v1-import` provenance per session, never claims native ownership, never overwrites a verified live mapping. `ImportArchiveResult` returns the exact 10-field count breakdown.
- QUALITY GO — The v1 read/write helpers are reused for the legacy path; v2 adds focused readers (`readLegacyMeta`/`readProviderTaskMeta`/`readProviderForkLinks`) and a self-contained resolve+collapse+collision layer. Mapping validation (malformed/provider-change/unknown-target/conflict/cycle) and content-collision detection run BEFORE `BEGIN`; the write path is one transaction with ROLLBACK-on-throw. The collision signature intentionally excludes the `updatedAt` bookkeeping timestamp so semantically-identical rows collapse (only real user-content differences conflict). Only `portable.ts`, `index-db.ts`, `index.ts`, `routes/portable.ts` + the two test files changed. Focused gates: engine portable 19/19, full engine 2145/2145, full server 233/233; engine typecheck + provider-index public-surface + server + web typecheck clean; `git diff --check` clean.
- SECURITY GO — The v2 bundle is authority-only and path-free: locators carry only a 64-hex home fingerprint, never a raw home or transcript path; a test scans the serialized default bundle for `/home/dev`, `filePath`, mirrored text, and hidden-reasoning markers and finds none. `filePath`/`cwd` survive ONLY inside the legacy v1 rollback shape (by design, for restore). Imports are bounded (array + UTF-8 caps reject bomb-like input before writes), schema-validated, transactional, and idempotent; a mapping collision or oversized field aborts with zero rows written (rollback proven). No credentials or provider cache rows are ever exported or imported.

## M5-T7-WEB-COMPAT independent gate record (2026-07-15, M5-T7-REVIEW)
- SPEC GO — Every Task 7 contract clause re-verified against source + re-run tests. compat-storage (`apps/web/src/lib/compat-storage.ts`): `readCompat` reads the DevHub key FIRST, then the EXACT legacy twin from `legacyKeyFor`, and on a legacy hit COPIES it forward to the DevHub key via a best-effort `setItem` WITHOUT deleting or rewriting the legacy value (test asserts `removeItem` len 0, legacy still present, the only `setItem` is the forward copy); `writeCompat` targets ONLY the DevHub key (test asserts legacy preserved, single `setItem`); `removeCompat` clears BOTH keys so a logout/empty-draft can't be resurrected by the read fallback (token end-to-end test proves a subsequent read is null); SSR (no `window` → null/no-op), quota (`setItem` throw non-fatal, still returns the found value), private-mode/disabled (`getItem` throw → null), and the `window.localStorage`-getter-throw case are all covered and non-fatal (`getStore` guards the getter access itself, not just the calls); `legacyKeyFor` maps `devhub-token`→`claude-ui-token` and `devhub:`→`claude-ui:` ONLY, returns null for any unmapped/legacy/empty key (no guessed mapping). router (`apps/web/src/lib/router.ts`): `providerTask` is an optional field appended in stable order (tab, project, session, providerTask — `buildRouteSearch` test asserts exact ordering); parse/build validate with a dependency-free grammar mirror `^pt1\.[a-z][a-z0-9-]*\.[0-9a-f]{64}\.[A-Za-z0-9_-]+$` (≤1024 chars) that I confirmed matches the engine's real serializer (`identity.ts`: `LOCATOR_PREFIX="pt1"`, `FINGERPRINT=/^[0-9a-f]{64}$/`, `BASE64URL=/^[A-Za-z0-9_-]+$/`, providers openai|anthropic both satisfy `[a-z][a-z0-9-]*`; `router.test.ts` feeds a REAL `serializeTaskLocator(...)` output through the validator to guard grammar drift); malformed and NUL-bearing (`pt1.openai.%00`) values are DROPPED without throwing; legacy URLs with no `providerTask` parse unchanged. Provider-owned browser state (`CodexNativePane.tsx`): draft localStorage scopes, `nativeTaskIdentity`, and the request-dedup `identityKey` all fold the raw home through `pathFreeHomeToken` (FNV-1a 32-bit → 8 hex) and percent-encode every part joined by a `.`/`|` that `encodeURIComponent` never emits — path-free AND NUL-free (tests assert the identity omits the raw home, contains the hashed token, and contains no ``, including a hostile `ab.c/d` native id). Eager App-imported bundle stays Node-free: `router.ts` imports only `react` (no engine locator module), and `vite build` succeeds with no Node-builtin externalization — the definitive proof the eager graph never pulls the engine's `node:fs`-transitive `serializeTaskLocator`.
- QUALITY GO — compat-storage is the single localStorage seam: 3 tiny pure functions + `getStore`/`legacyKeyFor`, each access double-guarded (window-access try/catch + per-call try/catch), well-documented intent (copy-on-read vs intentional remove-both). Every `claude-ui:*`/`claude-ui-token` holder was migrated to a `devhub:*` literal routed through the seam (App `devhub:ui`, FirstRun `devhub:onboarded`, Snippet `devhub:snippets`, Bookmarks `devhub:bookmarks`, Settings `devhub:conn`, Search `devhub:search-scope`, useTheme `devhub:theme`, useReducedMotion `devhub:perf-mode`, useRecentSessions `devhub:recent-sessions`, useDraft `devhub:draft:`, usePromptHistory `devhub:prompt-history:`, api token `devhub-token`); a repo grep finds ZERO remaining direct `claude-ui` key literals in authored non-test source (only accurate migration/legacy-name doc comments + the server-side `~/.claude-ui` data dir that M8 owns). No direct `localStorage` bypass of the seam except the pre-existing, out-of-scope `openai-pane-cwd` key (never in the claude-ui namespace, untouched by this commit). `pathFreeHomeToken` collision risk is scoped to in-memory single-home UI dedup and documented NOT security-sensitive. Real counts THIS review: web focused (compat-storage 22 + router 18 + CodexNativePane 53) = 93/93; FULL web suite 211/211; `tsc --noEmit` PASS (exit 0); `vite build` PASS; `git diff --check` clean; tree clean.
- SECURITY GO — Zero raw-home/NUL leak in authored source: a targeted scan of the six T7-authored files finds no NUL byte and no literal `` in non-doc source; every browser-persisted key and in-memory dedup identity folds the raw home through the 8-hex token, so no filesystem path lands in localStorage, a URL, or a DOM key; `providerTask` carries only the path-free serialized locator (64-hex fingerprint, never a raw home). The remaining `.home` references in `CodexNativePane.tsx` are in-memory equality checks and request bodies to the LOCAL server on the flag-OFF direct-key transport (the preserved rollback surface), not browser-persisted state — the token-key seam and provider-index facade keep the flag-ON path fingerprint-only. User-owned `ChatPane.tsx`/`SlashPalette.tsx` and `.gitignore`/`AGENTS.md` untouched. No P1/P2.
- VERDICT: GO. Contract fully met; no P1/P2. Two non-blocking P3 nits (do NOT block promotion, no tracked fix task required): (1) `AuthGate.tsx:16` doc comment still names the legacy `claude-ui-token` key though `setToken` now routes `devhub-token` through the seam (doc-only, functionally correct); (2) `pathFreeHomeToken` FNV-1a-32 has a theoretical cross-home collision that is documented as non-security-sensitive local-UI-only scoping. M5-T7-WEB-COMPAT is clear to promote to `campaign/auto-improve` under its normal promotion gate (flags remain false).

- `wip/devhub` at `641af8e` — remote-backed WIP with user-owned dirt and evidence; never merge wholesale.
- detached `m5-c0-schema` at `377a724` and `m5-cursor` at `82a8b74` — stale; do not build on them.

## M5-T9-STAGE-CUTOVER independent gate record (2026-07-15)
- SPEC GO — Fresh specification review against `.planning/devhub-codex-parity/m5-implementation-plan.md`. Every frozen invariant (1-12) and every Task 1-9 contract maps to a landed, tested surface: (1) native storage authoritative — no M5 code writes provider transcripts (archive v2 asserts 0 cache rows exported); (2) path-free public locator — `home-fingerprint.ts` SHA-256 of `devhub-home:v1\0<provider>\0<canonicalHome>`, `identity.ts` `pt1.` envelope with 64-hex fingerprint, canonical home only in `provider_homes`; (3) event identity synthetic/replayKey in `identity.ts`; (4) transactional replay idempotency in the store suites; (5-6) cache-vs-owned-state separation + additive-only metadata in `store-local-state`/portable v2; (7-8) writer-lease-fenced adapter mutations with the SINGLE shared `NativeTaskWriterLeaseStore` (Codex reuses Claude's exact seam, no second lease); (9) schema v14 base + v15 generation-epoch repair, rollback = ignore-not-down-migrate; (10) legacy sessionId never guessed (verified-legacy mapping only); (11) `claude-ui` -> `devhub` compat for URL/storage/env/archive/webhook; (12) `unifiedTaskIndex` default-on only after all gates — THIS task. Gate-2 induced-failure corpus 911/911. No missing requirement.
- ADVERSARIAL GO — No open P1/P2. Prior T7 P3 nits (AuthGate doc comment; documented non-security FNV `pathFreeHomeToken`) remain accepted. ONE new cutover-honesty P3 found and FIXED in the cutover commit: before, the settings surface could report `unifiedTaskIndex` applied while a fail-open coordinator init left the coordinator null; now `appliedDevHubFeatures.unifiedTaskIndex` is tied to `providerTaskIndexCoordinator !== null`, so the feature is reported available/applied only when the store exists AND the coordinator initialized. No unaccepted P3 remains.
- CHECKS GO — `evidence/m5/precutover-checks.md`: `git diff --check` clean; targeted secret scan clean in non-test source + evidence (only synthetic redaction fixtures live in `*.test.ts`); raw-home + NUL scan of evidence artifacts clean; generated-file check clean (no tracked dist/build/emitted js/d.ts); preservation hash/status clean (four user-owned paths untouched with recorded blob hashes; preservation matrix unchanged).
- STAGED CUTOVER — isolated branch `staging/m5-t9-cutover` commit `01f39e4` on reviewed base `f4d27fd`; flip is 6 files (feature-flags.ts, app.ts, and the 4 focused tests). Focused flag/settings/routes/rollback tests GREEN on that exact commit at `--maxWorkers=2` (engine 4/4, server 36/36, app.test.ts 80/80); engine+server+public-surface tsc PASS. Explicit stored-false rollback rehearsed. VERDICT: staged cutover clear; the default-on promotion + full exact-tip heavy-queue gate + Browser/Computer-Use QA remain the single [RONAK-GATE] human action. `nativeCodex`/`persistentClaude` stay false.

## M5-FINALIZE-CUTOVER integration record (2026-07-15) — [DONE on wip, NOT origin/main]
- INTEGRATION — Applied the reviewed `staging/m5-t9-cutover@01f39e4` diff (its 6 files) cleanly onto `wip/devhub-background-runner` over the docs tip `6631cef` (all 6 files were untouched by the docs commit, so `git apply --index --3way` applied with zero conflicts). `DEFAULT_DEVHUB_FEATURE_FLAGS.unifiedTaskIndex` is now true; `app.ts appliedDevHubFeatures.unifiedTaskIndex` is `providerTaskIndexCoordinator !== null` (honest applied truth; fail-open init reports disabled). `nativeCodex`/`persistentClaude` stay false.
- FULL HEAVY-QUEUE GATE (cache bypass) GREEN — `vitest run --no-cache` per package (workers pinned `--minWorkers=1 --maxWorkers=2`, since a bare `--maxWorkers` collides with vitest's default minThreads): engine 2204/2204 (79 files), server 236/236 (11 files), web 211/211 (13 files). `turbo run typecheck --force`: 5/5 packages PASS. `turbo run build --filter engine --filter server --filter web --force`: fresh vite build, no cache, all PASS.
- FULL-SUITE FALLOUT FIXED IN THIS COMMIT — the focused staging run (4 engine tests only) never ran the whole engine suite, so it missed `test/provider-index/gate2-drills.test.ts` which hard-coded `DEFAULT_DEVHUB_FEATURE_FLAGS.unifiedTaskIndex === false` with a "cutover has not happened" comment. Updated that assertion to the post-cutover truth (default now true) while preserving the test's real intent (explicit stored `unifiedTaskIndex:false` still wins over the on-default). This is the 7th changed file beyond the staged 6.
- ROLLBACK RE-PROVEN — `packages/server/test/provider-index-composition.test.ts` 3/3: (a) fresh engine, no override => onReady builds + initializes the coordinator exactly once and settings reports `unifiedTaskIndex` applied; (b) explicit stored `unifiedTaskIndex:false` persisted BEFORE ready => coordinator never built, settings reports the feature disabled, and a later re-enable builds it exactly once; (c) repeat-true / flip-to-false / flip-back-true never rebuild or tear down the single coordinator.
- DEVHUB-OWN BROWSER QA — served the BUILT bundle (`apps/web/dist`) via a repo-external static+proxy shim (`/tmp/qa-serve.mjs`, :5191) proxying `/api/*` to the built server (`tsx src/index.ts`, :8791), FULLY ISOLATED (`$HOME`/`CLAUDE_CONFIG_DIR`/`DEVHUB_DATA` all under a `mktemp -d` scratch dir with one 667-byte PROVISIONAL synthetic Claude session). The real `~/.claude/projects` (9.2G) + `~/.codex` (8.2G) were never scanned after isolation; no dataset writes. End-to-end `GET /api/settings` resolves `devHubFeatures.unifiedTaskIndex=true` (+ `requestedDevHubFeatures` true), `nativeCodex`/`persistentClaude` false. Home/Browse/Settings each rendered with 0 console errors; the only console noise is a benign pre-existing `apple-mobile-web-app-capable` PWA meta deprecation warning in the static `index.html`. Evidence: `evidence/m5/qa-note.md`, `evidence/m5/qa-live-settings.json`, `evidence/m5/qa-screenshots/{01-home,02-browse,03-settings}.png`. The first-party `com.openai.codex` Computer-Use gate stayed HELD (not exercised).
- CLEANLINESS — `git diff --check` clean (staged + unstaged); four user-owned preservation paths untouched (re-verified blob hashes: `.gitignore` 03479b6, `AGENTS.md` 28a7929, `apps/web/src/components/ChatPane.tsx` cfecb54, `apps/web/src/components/SlashPalette.tsx` 737110f).
- HELD (unchanged) — PRODUCTION release default-on promotion and the `origin/main` merge remain the [RONAK-GATE] human actions. This task landed the cutover on `wip/devhub-background-runner` only. STATE stays ACTIVE.

## M5-CUTOVER-FINALIZE independent gate record (2026-07-16)
- RECONCILIATION — `staging/m5-t9-cutover@01f39e4` (the isolated single-flag-flip branch) confirmed NOT an ancestor of this wip tip. wip already carries the identical resolved flip (landed independently at `M5-FINALIZE-CUTOVER` / `bfa8384`, above), re-verified under the FULL suite repeatedly since, a strict superset of the 36 focused tests staging ran. wip's flip supersedes the staged branch; no fast-forward/cherry-pick performed (nothing in the staged diff isn't already expressed on wip). `staging/m5-t9-cutover` branch lifecycle (delete/merge) is out of this task's scope.
- FULL EXACT-TIP HEAVY GATE (forced cache bypass, `--force`) GREEN — `turbo run test --filter engine --filter server --filter web --force`: engine 2206/2206 (79/79 files), server 237/237 (11/11 files — 236 pre-existing + 1 new), web 487/487 (34/34 files). `turbo run typecheck --force`: engine (`tsc --noEmit` + public-surface project) / server / web all PASS. `vite build` (web): succeeded, no errors (dist removed after, not a release artifact). `git diff --check`: clean. Full detail: `evidence/m5/gate-report.md`.
- ROLLBACK BYTE-COMPATIBILITY PROOF (new) — added
  `M5-CUTOVER-FINALIZE: legacy provider routes stay byte-compatible across the flag` to
  `packages/server/test/provider-index.test.ts`: two independent harnesses, one with an
  explicit stored `unifiedTaskIndex:false`, one on the applied-true default, both call the
  LEGACY `GET /api/providers/:provider/tasks` route and assert the raw response bytes are
  IDENTICAL (not just deep-equal), pinning the `observeListIntoCache`/`observeTaskIntoCache`
  side-effect-only contract in `provider-tasks.ts`. Passing (counted in the 237/237 above).
- DEVHUB-OWN BROWSER QA (new, real files this time) — a minimal hand-rolled fixture
  (`.planning/devhub-codex-parity/qa/m5-fixture-server.ts`, same pattern as the M3/M4
  fixtures — NOT `buildApp`, whose transcript/config watchers retry-loop against a
  synthetic temp home and OOM the process; observed this firsthand, twice, before
  switching patterns) registers the REAL `registerProviderTaskRoutes` +
  `registerProviderIndexRoutes` against a real `ProviderTaskIndexStore` + a real,
  initialized `ProviderTaskIndexCoordinator`. Vite dev served the real unmodified
  `apps/web` proxying to the fixture. Captured with gstack `browse` (Playwright-driven):
  Settings "Provider runtime status" table showing `Unified task index: Enabled / Active`
  at 1280×720 and 768×900 (no horizontal-overflow regression at either breakpoint, table
  row-height stable across breakpoints), plus a live in-page `fetch` round trip against
  `/api/provider-index/homes` and `/tasks` using the app's own stored auth token,
  returning real data (3 tasks, opaque locators, no raw home). Screenshots + full
  breakpoint/geometry table: `evidence/m5/browser-qa.md`,
  `evidence/m5/browser-{wide,narrow}-settings-provider-table.png`. First-party
  `com.openai.codex` Computer-Use QA excluded per scope (hard gate); fixture + dev server
  shut down cleanly after capture, no lingering processes.
- HELD (unchanged) — `origin/main` merge and the first-party Computer-Use QA remain
  [RONAK-GATE]/hard-gate. STATE stays ACTIVE.

## Recent checkpoints (last 3 tested commits on shared branch)
- `campaign/auto-improve tip`  Task 3 completion (T3B readThrough/projector + T3C rebuild + T3D server-composition wiring) promotion; engine provider-index 629/629, server 207/207, web provider-api 52/52, plus exact-tip SPEC/QUALITY/SECURITY GO. `unifiedTaskIndex` stays false.
- `c9a376b`  M5 coordinator observation-lanes promotion; 589/589 provider-index tests plus exact-tip SPEC/QUALITY/SECURITY GO.
- `cdbff7d`  Task 3 coordinator foundation/initialization promotion; 577/577 provider-index tests plus exact-tip SPEC/QUALITY/SECURITY GO.
