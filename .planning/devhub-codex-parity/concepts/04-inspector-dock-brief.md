Use case: ui-mockup
Asset type: DevHub desktop inspector concept
Governing input: Image 1 is the completed rich Codex task capture. Preserve the rail, header, open assistant transcript, resting composer, density, and measured inspector container language.
Primary request: Create a family plate of five side-by-side inspector surfaces on a plain `#181818` field. Each surface keeps the measured 300-unit width, 16 radius, `#2d2d2d` fill, 16 padding, compact rows, and one-surface container language. They are product component specimens, not cards or miniature dashboards. Label the family "DevHub · Review provider adapter diff · OpenAI · Codex" outside the panels.
Panel 1 "Diff": compact file list, restrained unified diff, paths "packages/engine/src/providers/types.ts" and "packages/server/src/provider-task-ws.ts", footer "2 files · +84 -19".
Panel 2 "Files": shallow project tree with the same two paths and one selected row.
Panel 3 "Terminal": provider-emitted output only, short lines "pnpm test" and "622 passed"; no shell input prompt implying an automatic unsandboxed terminal.
Panel 4 "Browser": simple page title, URL row, and muted empty viewport label "Not available for this task" because browser activity is capability-gated.
Panel 5 "Artifacts": compact rows "Build report", "Screenshot", and "No artifacts" as honest empty/available examples.
Every panel includes the same top tabs "Diff", "Files", "Terminal", "Browser", "Artifacts" with its own destination selected. A quiet footer says "Availability follows the task runtime".
Visible text (verbatim): "DevHub", "Review provider adapter diff", "OpenAI · Codex", "Diff", "Files", "Terminal", "Browser", "Artifacts", "packages/engine/src/providers/types.ts", "packages/server/src/provider-task-ws.ts", "2 files · +84 -19", "pnpm test", "622 passed", "Not available for this task", "Build report", "Screenshot", "No artifacts", "Availability follows the task runtime".
Classification: container placement and compact-row language are observed; Diff/Files/Terminal/Browser/Artifacts destinations and expanded dock behavior are proposed DevHub additions.
Style/medium: realistic code-native product UI; neutral dark palette; system monospace only inside the diff.
Composition/framing: landscape component-family plate; five tall inset inspector panels aligned on one baseline, each readable at the same measured proportions.
Constraints: one selected destination per panel; compact tabs; native scroll; no separate cards per file; no generated logos; no watermark.
Avoid: IDE clone, giant code editor, full-window terminal, neon syntax palette, split-pane clutter, generic dashboard.
Quality: high
