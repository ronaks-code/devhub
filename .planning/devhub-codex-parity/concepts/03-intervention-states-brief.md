Use case: ui-mockup
Asset type: DevHub desktop intervention-state concept
Governing input: Image 1 is the active Codex goal capture. Preserve the measured shell, transcript/composer alignment, and content-height inspector. Treat every new request/error state below as proposed and capability-gated, not observed first-party UI.
Primary request: Create a clean 2-by-3 product-state plate made from six readable transcript/composer crops in the measured DevHub style, not six miniature full windows. Each crop keeps the `#181818` open canvas, 736-wide narrative/composer alignment, system typography, and stable composer edge. Header strip is "DevHub" / "Add Claude task runtime" / "Anthropic · Claude".
State 1: inline permission request, not a modal: title "Permission request", action "Write", path "apps/web/src/features/tasks/api.ts", buttons "Allow", "Deny", "Cancel".
State 2: inline input request "Input requested" / "Which target should I verify?" with radio rows "Browser + desktop" and "Browser only", plus "Send response".
State 3: compact row "Tool failed" with action "Retry" and transcript retained.
State 4: narrow row "Reconnecting…" with actions "Check task status" and "Cancel".
State 5: terminal row "Request expired — no action taken"; it must never resemble approval.
State 6: "Cancelled by you" with the resting composer restored.
Composer: provider fixed, no provider picker; send disabled only while a blocking request is active. Do not show an "Always allow" action.
Visible text (verbatim): "DevHub", "Add Claude task runtime", "Anthropic · Claude", "Reconnecting…", "Check task status", "Cancel", "Permission request", "Write", "apps/web/src/features/tasks/api.ts", "Allow", "Deny", "Input requested", "Which target should I verify?", "Browser + desktop", "Browser only", "Send response", "Tool failed", "Retry", "Request expired — no action taken", "Cancelled by you".
Classification: all approval/input/error/reconnect UI is proposed and capability-gated. Claude file-allow is provider-contract proven under an SDK reference client but product-disabled until the selected raw CLI adapter passes M4. User input and approval variants are unproven.
Style/medium: shippable dark macOS app UI, code-native controls, restrained system typography.
Composition/framing: six equal readable crops on a plain `#181818` presentation field with small state labels outside product chrome; requests sit inline and use only subtle `#262626` surfaces.
Constraints: no modal AlertDialog for approval; timeout never accepts; no auto-retry of uncertain mutation; no provider switch; no logos; no watermark.
Avoid: giant warning cards, red full-screen banners, decorative status pills, excessive borders, dashboard grid, default browser controls, emoji.
Quality: high
