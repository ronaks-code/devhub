# DevHub Codex Parity Risk Register

Status values: `open`, `mitigating`, `accepted-conditional`, `closed`.

| ID | Risk | Likelihood / impact | Evidence | Mitigation and exit gate | Owner milestone | Status |
|---|---|---|---|---|---|---|
| R-01 | Existing dirty and untracked work is overwritten during the migration. | Medium / critical | 13 dirty/untracked files, including the raw OpenAI experiment. | Protected hash-verified backup; one writer per overlapping file; compare against baseline status and hashes at every milestone. | All | mitigating |
| R-02 | Raw OpenAI chat is mistaken for Codex and exposes arbitrary local bash/read/write. | High / critical | M2 removed tool advertisement/execution, locked exact development-only/not-Codex copy, aligned wire shapes, and added fail-closed tests. | Route remains explicit opt-in, requires configured Bearer/first-frame WS auth, never accepts URL tokens, and is never a Codex fallback. Retain/remove decision remains after M3. | M2-M3 | closed |
| R-03 | Localhost APIs are reachable cross-origin without auth. | High / critical | M2 raw OpenAI and provider mutations now reject no-token/query-token/untrusted-origin paths, but legacy local APIs still follow the optional global token model. | Preserve strict auth on new mutations; move the remaining legacy mutation surface to an authenticated desktop/browser channel and retest at M8. | M2, M8 | mitigating |
| R-04 | Codex schema presence is confused with installed/runtime support. | High / high | App-server is experimental; installed schemas include disabled/under-development features. | Generate bindings from installed binary, record version/hash, stable fallback, capability probe, opt-in experimental namespace, disabled UI for unproven methods. | M1-M3 | mitigating |
| R-05 | Codex-created tasks disappear from DevHub after restart. | High / high | `thread/list` defaults to `cli` and `vscode`, excluding `appServer`. | Always request `sourceKinds:["appServer",...]`; add restart/list/read contract test. | M1-M3 | open |
| R-06 | A Codex crash causes duplicate turns or unsafe replay. | Medium / critical | No current app-server supervisor or request correlation. | One process per effective `CODEX_HOME`; bounded peer; circuit breaker; on restart reread/resume and never replay uncertain `turn/start`. | M1-M3 | open |
| R-07 | Claude execution changes or violates the user's authentication/billing contract. | High / critical | Anthropic disallows third-party products from routing Free/Pro/Max credentials. A local API key was found and verified under isolated `CLAUDE_CONFIG_DIR`; default CLI auth remains `claude.ai`. | Require API/cloud auth at adapter startup, persist only redacted method/provider evidence, and fail closed rather than fall back to subscription OAuth. | M1, M4 | mitigating |
| R-08 | Claude approvals, input, fork, or reconnect controls lie about capability. | High / high | Provider controls worked while the SDK reference client owned the CLI child, but the selected raw persistent CLI peer has not reproduced multi-query, resume, permission, interrupt, post-interrupt resume, or fork continuation. | Keep controls false until M4 raw-peer fixtures/live gates pass; retain process-per-turn as visibly limited fallback; timeouts never accept. | M1, M4 | open |
| R-09 | DevHub-owned transcript/archive data becomes an alternate authority or outlives native deletion. | High / high | SQLite mirrors message text; gzip archive and portable import can reconstruct transcripts independently. | Classify all provider content as rebuildable cache; add invalidation/deletion semantics; never write native files; migrate export to reference/additive metadata where required. | M5, M8 | open |
| R-10 | Two DevHub or external clients write the same native task concurrently. | High / critical | No writer lease, revision check, or external mutation detection. | Lease `(provider, home, nativeTaskId)` with heartbeat/expiry; reread provider fingerprint before writes; refuse/reread on mutation; never claim control over unknown clients. | M1, M5, M7 | open |
| R-11 | Provider-agnostic IDs overwrite or merge tasks across providers. | High / critical | M2 introduced realpath-canonical, frozen `(provider, home, nativeTaskId)` keys, exact safe JSON-RPC correlation, foreign-event rejection, and effective-home-confined Codex fallback reads; legacy URL/SQLite records still lack the provider dimension. | Composite provider/native migrations, idempotent event IDs, and dual-read land in M5; provider remains immutable after create. | M2, M5 | mitigating |
| R-12 | Cross-provider fork leaks secrets, hidden reasoning, or credentials. | Medium / critical | No current redaction/preview contract. | Typed transferable-context allowlist, redaction, explicit preview, new native target session, additive bidirectional link, source hash unchanged. | M7 | open |
| R-13 | Packaged desktop looks built but cannot reach its API or spawn the server. | High / critical | Static bundle uses relative `/api`; production proxy is absent; default spawn package name is stale; any port 8787 is accepted. | Signed local IPC/loopback contract, strict health identity, sidecar packaging, closed-app launch/stream/resume smoke. | M8 | open |
| R-14 | DMG packaging remains red. | Medium / high | The M2 forced monorepo gate produced both the release `.app` and x64 DMG successfully after the earlier baseline failure; legacy identity remains. | Reproduce in M8 after Tauri identity/sidecar cleanup, then run closed-client smoke and signing/notarization gates. | M8 | mitigating |
| R-15 | Legacy `Claude UI` identity remains or breaking renames destroy compatibility. | High / medium | Tauri/TUI/PWA/localStorage/env/archive/webhook names remain legacy. | Remove user-facing copy; dual-read/alias persisted keys and envs; retain migration identifiers where changing them would lose data. | M5-M8 | open |
| R-16 | shadcn initialization overwrites brownfield CSS, `cn()`, aliases, or lockfile. | High / high | No config/alias; existing `--accent` is brand clay; large bespoke CSS. | Disposable copy preview, explicit Radix config, diff every dependency/CSS/config change, semantic token migration, add components one at a time. | Reference phase, M6 | mitigating |
| R-17 | `App.tsx` flag-day rewrite regresses dozens of hidden features. | High / critical | 1,729 lines; 93 server surfaces; many features are only partially reachable. | Strangler flags, compatibility facade, route/persistence matrix, bounded surface migrations, side-by-side gates before deletion. | M2-M8 | open |
| R-18 | Existing tests give a false green through Turbo cache. | High / high | M2 injected the overview clock, replaced calendar-drifting fixtures, and passed the forced 11/11-task monorepo gate. | Continue using `--force` for every milestone and add browser/desktop/protocol evidence. | M2-M8 | mitigating |
| R-19 | Protocol parser loses forward-compatible events or exhausts memory. | High / high | M2 normalizes known event families, keeps unknown browser events allowlisted, and retains bounded/redacted raw diagnostics backend-only; Codex/Claude peers remain. | Add bounded JSONL/stderr/queues plus malformed/oversized/duplicate torture fixtures in M3-M4. | M2-M4 | mitigating |
| R-20 | Approval timeouts accidentally authorize work. | Medium / critical | M2 added correlated request identity and finite 60-240s auto-resolution bounds; every timeout fixture is fail-closed. | Native M3/M4 bridges must preserve the shared policy: command/file/MCP cancel, permissions empty grant, user input empty only under declared bounded auto-resolution. | M2-M4 | mitigating |
| R-21 | First-party GUI synchronization is promised without support. | High / medium | Claude docs exclude `-p`/SDK tasks from pickers; Codex GUI source filtering is conditional. | Report SYNC-3 separately as verified-current-build, unsupported, blocked, or unknown; never block DevHub on it. | M1, reference | accepted-conditional |
| R-22 | Work mode implies Cowork interoperability. | High / medium | No documented Cowork session API. | Provider-neutral real-runtime mode, explicit folder/permission scope, never inspect private Claude Desktop storage, label Cowork interop unsupported unless documented. | M7 | accepted-conditional |
| R-23 | Reference imitation copies private assets/code or implies first-party status. | Low / critical | Goal asks for close visual parity. | Capture observable behavior only; code-native implementation; permitted marks only; explicit DevHub identity; no private storage or proprietary source extraction. | Reference, M6 | open |
| R-24 | Image generation invents or overrides observed Codex details. | Medium / high | Dense multi-state concept request. | Captures govern existing surfaces; ImageGen only fills DevHub/provider-neutral gaps; maximum 8+2; exact briefs and direct inspection. | Concept phase | open |
| R-25 | Browser/PWA/mobile scope expands beyond supported product behavior. | Medium / medium | Current shell has only partial narrow behavior. | Record supported minimum width/PWA rules from reference and repo; do not claim unsupported mobile parity; test declared viewports only. | Reference, M6-M8 | open |
| R-26 | Performance regresses under streaming and long transcripts. | High / high | Large transcript virtualizer and many dashboard modules already coexist. | Native scrolling, event coalescing only at fan-out, stable composer, rerender profiling, long-task/typing/stream cadence budgets. | M3-M8 | open |
| R-27 | Current product claims exceed actual reachability. | High / high | Campaign docs mark features complete that are server-only, broken, or untested. | Preservation matrix uses live route/UI tests rather than campaign prose; each row must be green or an intentional supported relocation. | All | mitigating |
| R-28 | M1 consumes excessive paid provider turns or modifies real private sessions. | Low / high | Claude used two normal turns plus one explicitly allowed unresolved-lifecycle turn; Codex used two. All known source/fork sessions were deleted through supported APIs/helpers. | Keep later live tests bounded and sequential; use synthetic fixtures first; clean only known IDs; never touch private first-party storage. | M1-M4 | mitigating |
| R-29 | Claude reports one selected model but bills or emits another. | Medium / high | Direct turn-1 init reported Haiku while `message_start`, assistant metadata, and `modelUsage` identified Sonnet 5; the SDK extra turn emitted Haiku. | Track requested/init/actual/billed model separately; fail or surface divergence; keep reliable model picker capability false until M4 explains or detects it. | M4 | open |

## Review cadence

- Re-evaluate this register at the end of every milestone.
- A risk closes only with a linked command, test, screenshot, or provider envelope.
- Conditional upstream limitations remain explicit and do not become simulated UI.
